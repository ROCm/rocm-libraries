# CCCL → rocThrust Sync: v3.0.3 → v3.1.4 — TODO

We are in the middle of a manual-port sync of upstream CCCL/Thrust commits
into rocThrust. There is no git merge in progress and never will be — this
sync is a series of hand-ported commits, landed as a single commit at the
end by `rocthrust-cccl-sync-finalize`. This file tracks which upstream
commits still need to be ported.

## Sync parameters

- **JIRA ticket**: none (no fixed project exists for rocThrust syncs)
- **Repo**: `ROCm/rocm-libraries` — rocThrust lives at `projects/rocthrust/`
- **Sync base branch** (`$SYNC_BASE`): origin/develop
- **Confirmed current tag** (`$CURRENT_TAG`): v3.0.3
- **Target tag** (`$TO_TAG`): v3.1.4
- **Sync branch** (`$SYNC_BRANCH`, currently checked out): users/jmahovsk/cccl-v3.1.0-sync-2
- **Investigation report**: cccl-investigation-v3.1.4.md
- **Path-prefix reminder**: upstream commits touch `thrust/thrust/*`
  (headers), `thrust/testing/*`, and/or `thrust/examples/*` — three siblings,
  not one path with the other two nested inside it. Each translates to the
  same-named local path under `projects/rocthrust/` (e.g. `thrust/testing/*`
  → `projects/rocthrust/testing/*`). Translate every path before applying.

## ⚠ Ordering rule

Commits below are listed **strictly oldest-first**. Only the first unticked
(`- [ ]`) item may be worked next. Do not skip ahead, reorder, or batch
multiple commits together — a later commit may assume an earlier one is
already applied (it may reference symbols the earlier commit introduces, or
fix a bug the earlier commit itself introduced).

## State of the sync

- No `git merge` is in progress. There is no `MERGE_HEAD`, and no conflict
  markers will ever appear as part of this process.
- Ticked (`- [X]`) items below mean the commit's changes are **staged**
  (`git add`), not committed. Everything lands in a single commit at the end
  of `rocthrust-cccl-sync-finalize`.
- Because this state can persist across sessions with nothing enforcing it
  (unlike RCCL's open merge, which git itself blocks you from abandoning
  silently), avoid `git reset --hard` on this branch, and consider a backup
  branch if the sync spans many sessions.

## Commits to port

<!-- One line per upstream commit, oldest-first, never reordered:
     - [ ] <sha> <subject> [SCOPE: HEADER,TEST,EXAMPLE] [⚠ touches <sensitive-files.md pattern>]
     SCOPE is a comma-joined subset of HEADER/TEST/EXAMPLE from
     rocthrust-commit-list.sh, naming which of thrust/thrust/,
     thrust/testing/, thrust/examples/ the commit touches. A TEST-only or
     EXAMPLE-only commit usually needs no CUDA/HIP source-level porting
     judgment — see rocthrust-cccl-sync-resolve/SKILL.md. -->
- [X] 4e5a7a46de21f180998c114b4ff20e6e3839f3bf Simplify Thrust iterator functions (#4178) [SCOPE: HEADER,TEST]
  - needs CUDA/HIP (libcxx) adaptation — gated `thrust/advance.h` and `thrust/distance.h` on `_THRUST_HAS_DEVICE_SYSTEM_STD`: device-std branch now does `using _THRUST_STD::advance/next/prev/distance;` via `_THRUST_STD_INCLUDE(iterator)` (mirrors the existing `swap.h` precedent), non-device-std branch keeps the pre-existing generic-dispatch implementation and its `.inl`/`system/detail/generic/*` files verbatim (upstream deleted these outright since it has no non-device-std fallback path at all)
  - deliberately diverged from upstream's disposition: kept `testing/advance.cu` and `testing/distance.cu` (upstream deleted both) since rocThrust's fallback code path still needs its own test coverage, unlike upstream's now-pure-alias implementation
  - test/ counterpart: no change needed — `test/test_advance.cpp` and `test/test_distance.cpp` continue to validate the same public `thrust::advance`/`next`/`prev`/`distance` API, unaffected by the internal implementation swap
- [X] 62970a104bb5907c936a9a52f77f3afbd6a3d711 Avoid instantiating discard_iterator while parsing (#4180) [SCOPE: HEADER]
  - clean port — `make_discard_iterator` now templated on `System = use_default`, applied verbatim to `thrust/iterator/discard_iterator.h`
  - clean port — added missing `#include <thrust/iterator/iterator_traits.h>` to `thrust/system/cuda/detail/core/load_iterator.h`; CUDA -> HIP counterpart: no HIP equivalent file exists (CUDA-only concept), confirmed by counterpart check
  - clean port — qualified `detail::allocator_aware_execution_policy` -> `thrust::detail::allocator_aware_execution_policy` in `thrust/system/cuda/detail/execution_policy.h`
  - HIP counterpart: proactively applied the same qualification fix to `thrust/system/hip/detail/execution_policy.h` (same unqualified construct in the same structural position) even though not functionally broken today, for consistency
- [X] 1a3ecb199a8e0a7ec97b21b8d5b0f5da5e9845d8 Remove `_CCCL_INLINE_VAR` (#4192) [SCOPE: HEADER]
  - N/A on AMD (already applied) — all 4 touched files (`raw_reference_cast.h`, `is_output_iterator.h`, `iterator_facade_category.h`, `retag.h`) already used `inline constexpr bool` locally, not `_CCCL_INLINE_VAR`; the mechanical rename this commit makes upstream was already in effect on this branch
  - category-8 sweep (porting-categories.md precedent — this exact commit): found and deleted 9 stale `// TODO(libhipcxx): replace inline with _CCCL_INLINE_VAR once libhipcxx gets ready` comments across the 4 files (matches real upstream PR #11296 which deleted the same stale comments); no local alias macro for `_CCCL_INLINE_VAR` existed; `.clang-format`'s `AttributeMacros` list never contained it — both other sweep points were no-ops
- [X] 60074e145c041609144bc1abed4d6cbc0598a5eb Make compiler version comparisons safer (#4185) [SCOPE: HEADER,EXAMPLE]
  - N/A on AMD (already applied) — all 42 touched files already use the parenthesized function-like `_CCCL_HAS_CUDA_COMPILER()`/`THRUST_HAS_CUDA_COMPILER()` form; the 3 shared files (`host_device.h`, `malloc_and_free.h`, `normal_distribution_base.h`) use rocThrust's own already-function-like wrapper `THRUST_HAS_CUDA_COMPILER()` (in `libcxx_wrapper/std/__cccl/compiler.h`, forwards to `_CCCL_HAS_CUDA_COMPILER()`); no bare usages found anywhere; no file changes needed
- [X] 45cb0b2110b0c2a7ad4a1543fc3f5188f4f1a6c0 Allow cuda::par*.on() to take cuda::stream_ref (#4225) [SCOPE: HEADER]
  - clean port — added `#include <cuda/stream_ref>` and `on(::cuda::stream_ref const&)` overloads to `execute_on_stream_base`, `execute_on_stream_nosync_base`, `par_t`, `par_nosync_t` in `thrust/system/cuda/detail/par.h` (CUDA-only file), verbatim
  - HIP counterpart: N/A — no `hip::stream_ref` or equivalent type exists anywhere in rocThrust/rocprim/hipcub; confirmed only pre-existing reference to `cuda::stream_ref` in the tree is `testing/cuda/stream_wrapper.cu` (a CUDA-only test file, untouched by this commit); `thrust/system/hip/detail/par.h` left unchanged
- [X] 1595d252c0f2b6124d52a4dbe5cb02c943d571b9 Drop `_CCCL_NO_VARIABLE_TEMPLATES` (#4229) [SCOPE: HEADER]
  - N/A on AMD (already applied) — `thrust/type_traits/logical_metafunctions.h` already has all 6 usings (conjunction/conjunction_v/disjunction/disjunction_v/negation/negation_v) unconditional with no guard, already routed through the `_THRUST_STD`/`_THRUST_STD_INCLUDE` translation layer (ahead of upstream's raw `::cuda::std`); no stale `_CCCL_NO_VARIABLE_TEMPLATES` references anywhere in the tree, not in `.clang-format`'s AttributeMacros either (not applicable — feature-test macro, not an attribute annotation)
- [X] 56e8daccc5a9964a480aa7bc47a674c35b9c6cd7 Simplify Thrust assign_value (#4227) [SCOPE: HEADER]
  - clean port — rewrote `thrust/system/cuda/detail/assign_value.h`'s two `assign_value` overloads to avoid instantiating `cuda_cub::copy`/`thrust::copy` for single-element assignment: added `detail::assign_value_kernel` + `detail::cross_system_assign_host_path`, switched to `trivial_copy_device_to_device`/`trivial_copy_from_device`/`trivial_copy_to_device` + `throw_on_error` + `triple_chevron`; all dependencies (`cross_system.h`, `util.h` helpers, `CCCL_DETAIL_KERNEL_ATTRIBUTES`) already present in the tree
  - translated upstream's `NV_IF_TARGET`/`NV_IS_HOST` to this file's existing `_THRUST_IF_TARGET`/`_THRUST_IS_HOST` wrapper (matches the file's pre-existing convention); kept `::cuda::std::` verbatim (not `_THRUST_STD`) matching this CUDA-only file's established convention (confirmed via other `system/cuda/detail/*.h` files, e.g. `dispatch.h`, `find.h`)
  - clean port — added the one-line FIXME(bgruber) comment to `thrust/system/cuda/detail/internal/copy_cross_system.h`, no functional change
  - HIP counterpart: not addressed this commit — flagged as "already changed since $SYNC_BASE" by counterpart check (pre-existing drift, not from this sync); no HIP-side equivalent rewrite attempted here since upstream's motivation (avoid instantiating thrust::copy/transform) is CUDA-specific plumbing
- [X] 57f72a1cef71933747777a6d20eec32bfb043806 Use `[[maybe_unused]]` in whole cccl (#4207) [SCOPE: HEADER,TEST,EXAMPLE]
  - clean port — mechanical `(void) x;`/`static_cast<void>(x)` -> `[[maybe_unused]]` swap across all 14 touched files (9 headers, 1 example, 4 tests); verified each file was still in the exact pre-commit "before" state before editing, using rocThrust's translated macro names where applicable (e.g. `THRUST_HOST_DEVICE`/`THRUST_HOST`/`THRUST_COMPILER` instead of upstream's `_CCCL_*`)
  - `contiguous_storage.h`: also dropped the `#if THRUST_COMPILER(GCC, <, 10)` dead-code block guarding the now-unneeded `(void) first; (void) last;` lines, matching upstream exactly
  - HIP counterpart: `agent_launcher.h`/`triple_chevron_launch.h` have no HIP counterpart (CUDA-only concepts); `memory_resource.h`'s HIP counterpart flagged as "already changed since $SYNC_BASE" (pre-existing drift, not touched here); `testing/*.cu` -> `test/*.cpp` counterparts likewise "already changed since $SYNC_BASE", left untouched
- [X] 8b4d386c91454c052c0e51aa32370d94909db875 Make `cuda::stream_ref` constructible on device (#4243) [SCOPE: HEADER]
  - clean port — merged the two `on()` overloads per class (execute_on_stream_base, execute_on_stream_nosync_base, par_t, par_nosync_t) in `thrust/system/cuda/detail/par.h` into a single `_CCCL_HOST_DEVICE on(::cuda::stream_ref s)` overload taking by value; dropped `_CCCL_EXEC_CHECK_DISABLE` from `execute_on_stream_base`'s constructor; matches upstream exactly, verified via diff
  - builds on commit 5's port (which first introduced the `::cuda::stream_ref` overloads to this file); HIP counterpart flagged as "already changed since $SYNC_BASE", not touched here
- [X] 66973d47f32208aa3813a483a0d800a1e1bc795b Drop `_CCCL_NODISCARD` (#4265) [SCOPE: HEADER]
  - clean port — replaced `THRUST_NODISCARD` (rocThrust's translated alias of `_CCCL_NODISCARD`) with the literal `[[nodiscard]]` attribute at the 8 usage sites this commit touches, across `device_allocator.h` and 7 `mr/*.h` files (`allocator.h`, `disjoint_pool.h`, `disjoint_sync_pool.h`, `fancy_pointer_resource.h`, `memory_resource.h` x2, `pool.h`, `sync_pool.h`)
  - category-8 sweep: `THRUST_NODISCARD` macro itself is NOT removed by this commit (only these 8 usage sites change) — confirmed still defined/used in `libcxx_wrapper/std/__cccl/attributes.h` and referenced elsewhere (e.g. `minimum.h`, `maximum.h`, `identity.h`, `not_fn.h`, `sequence_access.h`), so no macro-definition change needed; `.clang-format`'s AttributeMacros list already carries both `THRUST_NODISCARD` and `_CCCL_NODISCARD`/`_CCCL_NODISCARD_FRIEND` — left unchanged since `_CCCL_NODISCARD` is still forwarded-to by `THRUST_NODISCARD`'s definition, not eliminated; no stale TODO comments referencing `_CCCL_NODISCARD` found outside the translation-layer file itself
- [X] 9e8b4cb340397345f86ba96b19cd5c341d95b8c3 [Version] Update main to v3.1.0 (#4175) [SCOPE: HEADER]
  - deferred to finalize — pure upstream version bump (`THRUST_VERSION` 300000 -> 300100 in `thrust/version.h`); rocThrust tracks its own independent `THRUST_VERSION` (currently 300003 for v3.0.3); rather than bump per intermediate upstream version-bump commit, defer to `rocthrust-cccl-sync-finalize`, which should set `THRUST_VERSION` to 300104 once, reflecting the final `TO_TAG=v3.1.4` this sync targets; no file changes made here
- [X] ee57a8e2d419b070fe32a152460ee4b59a10f1b8 Use more libcu++ includes in thrust (#4316) [SCOPE: HEADER]
  - full single-pass port across all 91 touched files (upstream diff-tree showed 91, not the 82 originally estimated). Mechanical translations applied per established rules: `_CCCL_HOST_DEVICE`/`_CCCL_HOST` -> `THRUST_HOST_DEVICE`/`THRUST_HOST`, `_CCCL_COMPILER(X)` -> `THRUST_COMPILER(X)`, `::cuda::std::X` -> `_THRUST_STD::X`, `<cuda/std/HEADER>` -> `_THRUST_STD_INCLUDE(HEADER)`, and plain std headers upstream moved to `<cuda/std/HEADER>` (cstddef, cstdint, cstdlib, cassert, limits, utility, initializer_list, type_traits, cmath, cstring, cfloat) translated the same way locally
  - `_THRUST_STD::remove_cvref_t` is not portable on the `!_THRUST_HAS_DEVICE_SYSTEM_STD` fallback path (plain `::std::remove_cvref_t` is C++20-only); used the codebase's existing `::internal::remove_cvref_t<T>` (`detail/type_traits.h`) instead, in `allocate_unique.h`, `detail/reference.h`, `detail/pointer.h`, `detail/memory_algorithms.h`
  - `system/detail/sequential/trivial_copy.h`: did NOT collapse to upstream's unconditional single-path `::cuda::std::memmove` — preserved the existing `_THRUST_IF_TARGET`/host-device branch split (device path keeps calling `general_copy_n`); only translated the host branch's `memmove` call, the `<cstring>` include, and the doc comment
  - `system/hip/pointer.h`: added the matching translation even though HIP isn't in upstream's literal diff, for consistency with the cpp/cuda/omp/tbb sibling files
  - `detail/complex/c99math.h`: preserved the HIP-on-Windows-specific workarounds (`__HIP__` branch, `THRUST_HAS_HIP_COMPILER() && _MSC_VER`-guarded custom `log1p`/`log1pf`) that upstream doesn't know about, rather than collapsing the whole MSVC-fallback block to upstream's flat `using ::cuda::std::X` list; only translated the portable primitive calls within it (`<cmath>` include, `std::abs`, `std::complex`) — confirmed with user via AskUserQuestion before touching the file
  - `complex.h`/`complex.inl`: added `THRUST_COMPILER(NVRTC)` guards around `std::complex`/`sstream`/stream-operator code paths matching upstream (macro already exists in this codebase, resolves to always-false on HIP so these paths stay active); `std::complex<T>` qualified as `::std::complex<T>` per upstream (not `_THRUST_STD::complex`, since `std::complex` isn't part of libcu++/rocPRIM's device-std surface)
  - `system/detail/generic/copy_if.inl`: no edit needed — already has a ROCm-specific structure (unconditional `_THRUST_STD_INCLUDE(limits)` + a conditional extra `<limits>` under `_THRUST_HAS_DEVICE_SYSTEM_STD`) that resolves the same dedup concern differently from upstream's diff
  - 24 `system/cuda/detail/*.h` + `system/cuda/pointer.h` files: upstream's diff applied verbatim via `git apply` (CUDA-only files, not portable)
  - all 91 files verified staged with `git diff --cached`/`git diff` cross-check against the authoritative `git diff-tree --no-commit-id --name-only -r ee57a8e2d419b070fe32a152460ee4b59a10f1b8` file list
- [X] 72692d32d26ead0dbf1ee53883fde9e5de0e10c5 Remove extra semicolons (#4426) [SCOPE: HEADER,TEST]
  - purely mechanical: removed 9 stray trailing `;` after `{}` bodies/macro invocations across 8 files (copy.cu, testframework.h, contiguous_storage.h, head_flags.h, tail_flags.h, vector_base.h ×2, bad_alloc.h, system_error.h), matching upstream's diff exactly (8 files, 10/10 line changes); no `_CCCL_`/`::cuda::std::` translation involved; all 8 files staged
- [X] bd60840e829b3f3355d103acfd3854af097230cc Attempt to recover from upstream OOM in disjoint_pool. (#4420) [SCOPE: HEADER,TEST]
  - `mr/disjoint_pool.h`: ported verbatim (pure C++, no CCCL-specific syntax in the functional change) — new `squeeze()` method that frees unused cached chunks/oversized allocations, `do_allocate` wrapped in try/catch(`std::bad_alloc`) to retry after squeezing, new `pool_idx` field on `chunk_descriptor`, `bucket_idx` renamed to `pool_idx` throughout; only CCCL-specific bits translated: `::cuda::std::max/min` → `_THRUST_STD::max/min`, `<cuda/std/__algorithm/max.h,min.h>` → `_THRUST_STD_INCLUDE(__algorithm/max.h),(min.h)` (established pattern, already used in extrema.h/vector_base.inl), `#include <thrust/find.h>` added verbatim, bare `find`/`find_if` calls resolve via ADL matching existing local convention in this file
  - `testing/mr_disjoint_pool.cu`: ported verbatim — new `TestDisjointPoolSqueeze` test + `dummy_resource` free/used-byte and allocation-id tracking; all assertion macros used (`ASSERT_THROWS`, `ASSERT_NOT_EQUAL`, `ASSERT_LEQUAL`, `ASSERT_EQUAL_QUIET`, `ASSERT_NOT_EQUAL_QUIET`) and `thrust::system::detail::bad_alloc` already existed locally; the pre-existing `operator==` was already `THRUST_HOST_DEVICE` locally (no translation needed there)
  - verified: diff stat matches upstream exactly (319 insertions/29 deletions across both files)
- [X] c796a7ffff37b713cda08b607c2ed444f4e6b190 Move libcudacxx endian macros to cccl (#4429) [SCOPE: HEADER] ⚠ touches sensitive-files.md pattern
  - deliberate no-op: 5 of 6 changed files are under `libcudacxx/` (CCCL/libcu++'s own tree, out of scope for a rocThrust-only sync). The only rocThrust file touched, `thrust/detail/config/cpp_dialect.h`, collapses its MSVC-vs-GCC/clang `THRUST_COMP_DEPR_IMPL` pragma-warning branch into a call to a brand-new `_CCCL_WARNING` macro that this same commit adds in `libcudacxx/include/cuda/std/__cccl/compiler.h` — that macro doesn't exist anywhere in this checkout (not in rocThrust, not in rocPRIM's vendored libcu++-equivalent headers). Confirmed via user decision: leave `THRUST_COMP_DEPR_IMPL`'s existing if/else structure untouched — it's a pure code-style consolidation upstream with zero functional difference in emitted warning behavior, and inventing a new local `THRUST_WARNING` macro isn't warranted since nothing else needs it
- [X] 25a904960386202f2d8bd0da566833d35eb48b1e Improve some thrust iterators (#4461) [SCOPE: HEADER,TEST,EXAMPLE]
  - `iterator/zip_iterator.h`: added a new unconditional variadic constructor (`zip_iterator(Iterators&&...)`, SFINAE-gated via `_THRUST_STD::enable_if_t`/`is_constructible_v`, forwarding via `_THRUST_STD::forward`) and CTAD deduction guide (`zip_iterator(Iterators...) -> zip_iterator<thrust::tuple<Iterators...>>`) — both work correctly in both `_THRUST_HAS_DEVICE_SYSTEM_STD` and legacy-fallback modes with no `#if` branching needed, because `thrust::tuple<T...>` is confirmed (via `thrust/tuple.h`) to already be a template alias for `_THRUST_STD::tuple<T...>` in device-std mode and the distinct custom fallback class otherwise — so it's the mode-correct deduced type either way, and `_THRUST_STD::is_constructible_v`/`forward` are generic traits that work against either tuple type. Simplified both pre-existing `#if`/`#else`-branched `make_zip_iterator` overloads to direct-list-init (`zip_iterator<...>{t}` / `{its...}`) instead of recursive `make_tuple` calls, keeping the existing per-mode branching structure intact.
  - `iterator/constant_iterator.h`: applied upstream verbatim — default constructor changed to `= default`, removed unused `protected: value_reference()` accessors (confirmed unused anywhere else in the local codebase via repo-wide grep), `Value m_value` → `Value m_value{}`, added CTAD deduction guide `constant_iterator(ValueT) -> constant_iterator<ValueT>`.
  - ~33 remaining files (6 examples, 9 testing, 1 allocator .inl, 1 cuda/detail/unique.h, 13 system/detail/generic/*.inl, 2 system/detail/sequential/*.inl, zip_function.h): purely mechanical `make_zip_iterator(make_tuple(...))` → `make_zip_iterator(...)` call-site simplifications (no CCCL-specific syntax involved) — confirmed via full diff review that no other logic changes were hiding in these files; deliberately preserved upstream's own inconsistency in `examples/padded_grid_reduction.cu` (only one of its two occurrences was simplified upstream, the other left wrapped in `make_tuple`) to match upstream's diff exactly.
  - Added the two new tests verbatim: `testing/constant_iterator.cu`'s CTAD assertion in `TestMakeConstantIterator` (using `_THRUST_STD::is_same_v`, established local convention) and `testing/zip_iterator.cu`'s new `TestZipIteratorConstructionFromIterators` struct.
  - Verified: all 37 files match upstream's file list and diff stats exactly (191/155 vs upstream's 192/156 — the 1-line difference is an expected, harmless artifact of zip_iterator.h's pre-existing dual-mode `#if`/`#else` structure needing more touched lines than upstream's single-mode file for the same logical change).
- [X] 9da054a4238d3b149f466cca403e3226e87a9d86 Deprecate more `Thrust` facilities in favor of `libcu++` ones (#4334) [SCOPE: HEADER,TEST,EXAMPLE]
  - Large commit (228 upstream files; 209 in scope for rocThrust after excluding cub/cudax/libcudacxx/docs/thrust-benchmarks). Upstream deprecates the Thrust-native functors/iterator-helpers that now have direct `::cuda::std::`/`::cuda::` equivalents (`equal_to, not_equal_to, greater_equal, greater, less_equal, less, plus, minus, multiplies, divides, modulus, negate, logical_and, logical_or, logical_not, bit_and, bit_or, bit_xor, maximum, minimum, distance, advance, next, prev, not_fn`) and repoints every internal call site at the replacement.
  - ~11 core `thrust/thrust/*.h` files got the actual deprecation-wrapper implementations (`advance.h`, `distance.h`, `functional.h`, `binary_search.h`, `detail/functional/operators.h`, `detail/type_traits/is_commutative.h`, `iterator/constant_iterator.h`, `iterator/transform_input_output_iterator.h`, `iterator/zip_iterator.h`, `type_traits/is_operator_less_or_greater_function_object.h`, `type_traits/is_operator_plus_function_object.h`): ported using the established `_THRUST_STD`/`_THRUST_HAS_DEVICE_SYSTEM_STD` dual-path convention (e.g. `advance.h`/`distance.h` add `THRUST_DEPRECATED_BECAUSE("Use _THRUST_STD::X instead")`-tagged wrappers forwarding to `_THRUST_STD::X`, matching upstream's `CCCL_DEPRECATED_BECAUSE("Use ::cuda::std::X instead")` forwarding to `::cuda::std::X`); `thrust::maximum`/`minimum`/`not_fn` call sites use the pre-existing local `::internal::` shim (portable across CUDA/HIP) rather than upstream's CUDA-only `::cuda::maximum`/`::cuda::std::not_fn`, consistent with how this port already handles those two functors elsewhere.
  - 13 further `thrust/thrust/*.h` files needed only a doc-comment/example reflow (no behavior change): `adjacent_difference.h`, `inner_product.h`, `merge.h`, `mismatch.h`, `reduce.h`, `scan.h`, `set_operations.h`, `sort.h`, `tabulate.h`, `transform.h`, `transform_reduce.h`, `transform_scan.h`, `unique.h`. `sort.h` additionally drops a now-dead `// TODO(bgruber)` comment and 4 redundant `is_same<CompareOp, thrust::less/greater<Key/void>>` checks from `can_use_primitive_sort`, matching upstream — the checks became pure duplicates of the adjacent `::cuda::std::` checks once the functors alias.
  - Remaining 185 files (testing/examples/system-detail call sites) were mechanical `thrust::X` → `_THRUST_STD::X` (or `::internal::X` for `maximum`/`minimum`/`not_fn`) call-site swaps, applied via a scripted regex pass plus a manual follow-up for ~16 files where the same functors were referenced *unqualified* (bare `equal_to<...>()`, `less<...>()`, etc.) from inside `thrust::`/`thrust::cuda_cub::` namespaces and so weren't matched by a `thrust::`-prefixed regex: `testing/zip_iterator_reduce_by_key.cu`, `thrust/detail/range/{head_flags,tail_flags}.h`, and `thrust/system/cuda/detail/{adjacent_difference,count,equal,extrema,inner_product,merge,mismatch,reduce,reduce_by_key,set_operations,sort,unique,unique_by_key}.h`; `thrust/system/tbb/detail/merge.inl` needed only a clang-format reflow after the script's substitution overflowed 4 lines past 120 cols.
  - Final verification pass (full re-grep of all 209 files for both `thrust::`-prefixed and bare occurrences of the tracked functor set, plus a per-file upstream-vs-local `--numstat` diff cross-check) caught 3 more gaps the script's `thrust::`-literal regex couldn't see, all now fixed: (1) `testing/functional.cu`, `functional_arithmetic.cu`, `functional_logical.cu` used the tracked functors as a *macro parameter* (`thrust::operator_name<data_type>` / `std::operator_name<data_type>`) — fixed to `::cuda::std::operator_name<data_type>` / `::std::operator_name<data_type>`, matching upstream exactly; (2) `testing/unittest/assertions.h` used the `THRUST_NS_QUALIFIER::` qualifier instead of `thrust::` for `distance`/`min`/`equal_to` — fixed to `_THRUST_STD::`; (3) `thrust/system/cuda/detail/set_operations.h` had 2 `thrust::distance` calls the script's pass missed — fixed to `_THRUST_STD::distance`. Remaining per-file `--numstat` diffs against upstream (~20 files, mostly `testing/cuda/*.cu` and the 11 core files) are confirmed benign: `_THRUST_STD::` is shorter than `::cuda::std::`, so lines that upstream had to wrap across multiple lines fit on one line locally, producing fewer inserted/deleted lines for identical semantic content (spot-checked several, including `binary_search.h`'s doc-comment examples and `set_operations.h`'s `distance` fix, which now matches upstream's stat exactly: 11 insertions/10 deletions).
  - All 209 in-scope files staged.
- [X] fbf517d567badc1da166baa674c741353f119f0b [Thrust] Perform asynchronous allocations by default for the `par_nosync` policy (#4204) [SCOPE: HEADER]
  - deliberate no-op: upstream touches exactly 1 file, `thrust/thrust/system/cuda/detail/temporary_buffer.h`, adding `cudaMallocAsync`/`cudaFreeAsync`-based overloads (with `cudaMalloc`/`cudaFree` fallback) for `par_nosync_t`/`execute_on_stream_nosync`. Both the local CUDA-mirror file (`projects/rocthrust/thrust/system/cuda/detail/temporary_buffer.h`, confirmed byte-identical via diff against upstream's post-commit blob) and its HIP-native counterpart (`projects/rocthrust/thrust/system/hip/detail/temporary_buffer.h`, using `hipMallocAsync`/`hipFreeAsync`, `hip_rocprim` namespace, `_THRUST_STD::ptrdiff_t`, AMD copyright header) already carry this exact feature — last touched by the prior baseline commit `1eb022d06b7 feat(rocthrust): CCCL 3.0.x changes (#3773)`, before this sync began. Nothing to stage.
- [X] 8da9b8fcbdec6878e03c55da62bbd08468e8be27 Use `cudaStream_t` for `thrust::device.on(...)`. (#4451) [SCOPE: HEADER,TEST]
  - `thrust/system/cuda/detail/par.h`: reverted the `::cuda::stream_ref` migration back to plain `::cudaStream_t` in all 4 `.on(...)` overloads (`execute_on_stream_base::on`, `execute_on_stream_nosync_base::on`, `par_t::on`, `par_nosync_t::on`) — parameter type `::cuda::stream_ref s` → `::cudaStream_t s`, body `s.get()` → `s`, and removed the now-unused `#include <cuda/stream_ref>`. Post-fix diff against upstream's target blob is clean (only pre-existing, unrelated `base_t(){}`  vs `base_t() {}` whitespace differences from before this sync).
  - `thrust/system/hip/detail/par.h`: no change needed — HIP's `.on(...)` overloads always took `hipStream_t const&` directly and never adopted `cuda::stream_ref`, so this commit doesn't apply to the HIP backend.
  - `testing/cuda/stream_wrapper.cu` (upstream's new 87-line test): already present locally, byte-identical to upstream's version (plus the standard AMD copyright header) — apparently carried in via the `v3.0.3` baseline. It could not have compiled against the un-fixed `par.h` (its `stream_wrapper`/`stream_wrapper_ref` helpers rely on the `cudaStream_t`-taking overload to avoid a 2-hop implicit conversion), so the `par.h` fix above was a real, necessary correctness fix, not a cosmetic one.
- [X] ceba82d43c01681c9aa051dbf030f3fe1bc8fd6b Missing forward include in iterator facade category (#4512) [SCOPE: HEADER]
  - deliberate no-op: upstream adds one missing `#include <thrust/iterator/detail/iterator_category_to_system.h>` to `thrust/iterator/detail/iterator_facade_category.h`. Local file already has this include at line 33 (present since an earlier baseline/port). Nothing to stage.
- [X] 2b5bde83f113a65438820b28416f3988e51e7d58 Drop superfluous semicolons (#4435) [SCOPE: TEST]
  - Of the 38 upstream files touched, only 3 are in scope (rest are `cub/`/`cudax/`/`libcudacxx/` — out of scope): `testing/address_stability.cmake` and `testing/cuda/transform_iterator.cmake` each got the added comment `# There are extra ; in the extended lambda implementation`; `testing/unittest/exceptions.h` had its stray `};` closing-brace semicolon after `namespace unittest` dropped to `}`. Note: `test/address_stability.cmake` (singular `test/`, a separate legacy ROCm directory last touched by the CCCL 3.0.0 base import, not the `thrust/testing/` mirror) is out of scope and left untouched.
  - All 3 files staged.
- [X] 7eedb6e38ef188d8a60e513fb1c287bbb12550a3 [Thrust] Add `reduce_into` (#4355) [SCOPE: HEADER,TEST]
  - New `reduce_into` API (void-returning `reduce` variant that writes to an output iterator, async under `par_nosync`) ported across all 6 in-scope files, verified file-by-file against upstream's exact numstat: `thrust/reduce.h` (+353, public API decls + doc comments — translated the doc-comment example's `cuda::maximum{}` to `_THRUST_LIBCXX::maximum<int>()` and `#include <cuda/functional>` to `#include <thrust/functional.h>`, matching this file's own pre-existing adjacent `reduce` doc example convention), `thrust/detail/reduce.inl` (+81, dispatch-layer + free-function overloads using local `THRUST_EXEC_CHECK_DISABLE`/`THRUST_HOST_DEVICE` macros), `thrust/system/detail/generic/reduce.h` (+17, declarations), `thrust/system/detail/generic/reduce.inl` (+42/-9, generic default impls plus upstream's own `InputType`→`T`/`RandomAccessIterator`→`InputIterator`/`OutputType`→`T` renames on the existing `reduce` overloads; used `_THRUST_STD::plus<T>()` matching this file's own established line-58 convention), `thrust/system/cuda/detail/reduce.h` (+99/-24, new `get_reduce_n_temporary_storage_size` helper factored out of `reduce_n_impl`, new `reduce_n_into_impl`/`reduce_n_into`/3 `reduce_into` overloads using `_THRUST_STD::distance`/`_THRUST_STD::plus<T>()` matching this file's existing `reduce`/`reduce_n` convention), `testing/reduce.cu` (-9, dropped now-unused `is_equal_div_10_reduce` struct).
  - New file `testing/reduce_into.cu` added (upstream 205 lines + 17-line AMD copyright header = 222 total), covering `TestReduceIntoSimple`, dispatch (`my_system`/`my_tag`), `TestReduceInto` over `IntegralTypes`, mixed-type host/device, `TestReduceIntoWithOperator`, `TestReduceIntoWithIndirection` (`plus_mod3` lookup table), `TestReduceIntoCountingIterator` — ported verbatim with `_CCCL_HOST_DEVICE`→`THRUST_HOST_DEVICE` to match this file's macro era.
  - HIP backend deliberately untouched: HIP's `reduce.h` has no `reduce_n_into`/`reduce_into` (neither does upstream's HIP-equivalent surface — this was a CUDA-only upstream PR), so HIP execution policies fall through to the generic `system::detail::generic::reduce_into` fallback automatically, exactly like every other non-CUDA backend upstream.
  - All 7 files staged. Small numstat deltas beyond upstream's own diff (`reduce.cu` +1 vs upstream's clean -9; `reduce.h` +8/-8; `system/cuda/detail/reduce.h` +3/-3; `system/detail/generic/reduce.inl` +1/-1) are pre-existing staged changes from earlier commits' translation-convention work already accumulated on this branch (e.g. `thrust::distance`→`_THRUST_STD::distance`, `thrust::plus`→`_THRUST_STD::plus`, `#include <cstdint>`→`#include <cuda/std/cstdint>`), confirmed line-by-line, not errors introduced by this commit's port.
- [X] 58e329d12cae8bd73bead7524247f8f24e013a48 Implement `shuffle_iterator` iterator type (#4564) [SCOPE: HEADER,TEST]
  - New public `shuffle_iterator<IndexType, BijectionFunc>` fancy iterator that generates a pseudorandom permutation on the fly (for permuting/sampling without materializing a shuffled buffer). Confirmed via `find` that shuffle has always been entirely generic — no CUDA-specific or HIP-specific shuffle files exist, matching upstream's file list exactly (no backend files touched by this commit either).
  - New file `thrust/detail/random_bijection.h` (177 lines, matches upstream exactly): extracts the `feistel_bijection` Feistel-cipher class (previously private to `system/detail/generic/shuffle.inl`) into a public reusable header, and adds a new `random_bijection<IndexType, Bijection>` adaptor that iterates any power-of-two bijection until the result is `< n`, supporting arbitrary (non-power-of-two) problem sizes. Translated `_CCCL_HOST_DEVICE`→`THRUST_HOST_DEVICE`, `<cuda/std/X>`→`_THRUST_STD_INCLUDE(X)`, `::cuda::std::X`→`_THRUST_STD::X`, and `_CCCL_ASSERT(false, "...")`→bare `assert(false && "...")` (confirmed `_CCCL_ASSERT` has zero precedent anywhere in this codebase; bare `assert()` is the established convention, e.g. `thrust/detail/contiguous_storage.h:142` and this same file's sibling `shuffle_iterator.h::dereference()`).
  - New file `thrust/iterator/shuffle_iterator.h` (184 lines, matches upstream exactly): the `shuffle_iterator` class itself (via `iterator_adaptor`) plus `make_shuffle_iterator` factory. Same translation conventions applied; doc-comment code examples left verbatim (already `thrust::`-only, no `cuda::` namespacing to translate).
  - `thrust/system/detail/generic/shuffle.inl` (+5/-87 locally vs upstream's own +4/-86): removed the now-redundant private `feistel_bijection` class (86 lines) in favor of `#include <thrust/detail/random_bijection.h>`, and repointed `construct_key_flag_op`/`shuffle_copy` to use `thrust::detail::feistel_bijection`. The extra +1/-1 versus upstream's diff is `#include <cstdint>`→`#include _THRUST_STD_INCLUDE(cstdint)`: confirmed via `git show HEAD:...shuffle.inl` that this line was still untranslated on our baseline (upstream's own file already had `<cuda/std/cstdint>` here before this commit, so upstream's diff never touches it) — legitimate in-scope translation-convention cleanup, not an error.
  - `testing/shuffle.cu`: parameterized every existing test (`TestShuffleSimple`, `TestShuffleCopySimple`, `TestHostDeviceIdentical`, `TestFunctionIsBijection`, `TestShuffleKeyPosition`, `TestShuffleUniformPermutation`, `TestShuffleEvenSpacingBetweenOccurances`, `TestShuffleEvenDistribution`) into `...Base<ShuffleFunc, ...>` templates driven by both the existing `thrust::shuffle`/`shuffle_copy` functions and the new `shuffle_iterator` (via new `...Iterator` variants and dispatch structs `iterator_shuffle`/`iterator_shuffle_copy`/`thrust_shuffle`/`thrust_shuffle_copy`); renamed `TestBijectionLength`→`TestFeistelBijectionLength` (now referencing `thrust::detail::feistel_bijection` instead of the old `system::detail::generic::feistel_bijection`); added 3 new tests (`TestShuffleIteratorConstructibleFromBijection`, `TestShuffleAndPermutationIterator`, `TestShuffleIteratorStateless`) and a new `TestFunctionIsBijectionIterator` using `thrust::detail::random_bijection<uint64_t>`. Verified via full-file `diff` against upstream: byte-identical body aside from the pre-existing AMD copyright header — a zero-deviation port, including upstream's own apparent oversight of omitting `DECLARE_INTEGRAL_VECTOR_UNITTEST(TestShuffleEvenDistributionIterator);` (mirrored deliberately, not "fixed", since this is a faithful port).
  - All 4 files staged. Aggregate cached stat 582/119 vs upstream's 581/118 — the sole +1/-1 delta is the `shuffle.inl` include-line cleanup documented above, confirmed line-by-line, not an error.
- [X] 83ea8015adfe80bad055067e6af4022b550aad15 Move NVTX to libcu++ and add support for Thrust (#4537) [SCOPE: HEADER,TEST]
  - Deliberate skip, out of scope: upstream inserts `_CCCL_NVTX_RANGE_SCOPE("thrust::algo_name")` calls at ~35 dispatch-layer call sites across `thrust/thrust/detail/*.inl`, backed by new/modified libcudacxx headers (`cuda/__nvtx/nvtx.h`, `cuda/__nvtx/nvtx3.h`) that define the macro. Confirmed this repo has no libcudacxx-equivalent project at all (only `hipcub`, `hiprand`, `rocprim`, `rocrand`, `rocthrust`), and grepped `rocthrust`/`rocprim`/`hipcub` for `NVTX`/`nvtx`/`roctx`/`ROCTX` — zero hits anywhere. Adding real GPU profiling instrumentation (a roctx-based backend, or even a no-op stub macro) is a separate infrastructure decision, not a mechanical port of this commit's diff. Nothing staged.
- [X] 07729d9a7d90415684e7f2c17d42ca21518a19ee Drop host STL includes in CUB if there are libcu++ alternatives (#4619) [SCOPE: TEST]
  - Only 1 of 32 upstream files is in scope (rest are `cub/`/`libcudacxx/` — out of scope): `testing/cuda/mismatch.cu` gets `#include <cuda/std/atomic>` and `std::atomic<size_t>`→`cuda::std::atomic<size_t>`, left untranslated (not `_THRUST_STD::`) matching this file's own existing precedent (line 143 already uses bare `cuda::std::equal_to<T>()`, and sibling files `testing/cuda/offset_iterator.cu`/`sort.cu` already use literal `<cuda/std/iterator>`/`<cuda/std/limits>` includes) — confirmed the `_THRUST_STD` translation convention applies to backend-agnostic internal implementation code, not `testing/cuda/`-scoped test files exercising the CUDA path directly.
  - Staged. `git diff --numstat` = 3/1, exact match to upstream.
- [X] 6887634f24fd5a27f271b84d1517a16cd8789bd2 Replace use of `__CUDACC__` with `_CCCL_CUDA_COMPILATION()` (#4587) [SCOPE: HEADER,TEST]
  - Of 21 upstream files, 2 are in scope (rest are `cuda experimental`/`libcudacxx` — out of scope): `thrust/system/cuda/detail/cdp_dispatch.h` translated `defined(__CUDACC__) && defined(__CUDA_ARCH__)`→`THRUST_DEVICE_COMPILATION()`, using this codebase's own bridging wrapper macro (defined in `thrust/detail/libcxx_wrapper/std/__cccl/compiler.h`, forwarding to the real `_CCCL_DEVICE_COMPILATION()` once available) rather than the bare `_CCCL_*` name directly, matching the file's existing macro era and confirmed transitively available via `thrust/detail/config.h`.
  - `testing/vector.cu`: deliberate no-op. This HIP port has already fully dropped the `#if defined(__CUDACC__) && CUDART_VERSION == 3000` WAR block from both `TestVectorResizing`/`TestVectorReserving` (confirmed via `Read` — the try/catch bodies run unconditionally, with `hipGetLastError()` already substituted in place of `cudaGetLastError()`) — there is no guard construct left locally for this commit's rename to apply to.
  - `cdp_dispatch.h` staged; `git diff --numstat` = 3/3, exact match to upstream.
- [X] 64a6f335f50888423dcc04f13f5f9cb0fb32b7bb Add CUDA toolkit macros (#4630) [SCOPE: TEST]
  - Deliberate no-op: only 1 of 17 upstream files in scope, `testing/cuda/sort.cu`, renaming `_CCCL_CUDACC_AT_LEAST(12, 2)`→`_CCCL_CTK_AT_LEAST(12, 2)`. Confirmed the local file already uses `_CCCL_CTK_AT_LEAST(12, 2)` (line 306/315) and the macro is already defined in `thrust/detail/libcxx_wrapper/std/__cccl/cuda_toolkit.h` — zero uncommitted diff for this file. Already at the post-commit state; nothing to stage.
- [X] a0a7b3856e9110722fec1d9ed474648251de033d Reduce the use of `__CUDA_ARCH__` (#4589) [SCOPE: HEADER]
  - Of 40 upstream files, 4 are in scope (rest `cub`/`cudax`/`libcudacxx` — out of scope). Unlike commit 26 (`cdp_dispatch.h`, still on the older `THRUST_*`/`_THRUST_IF_TARGET` macro convention), these 4 files already use bare `_CCCL_*` macros directly (`_CCCL_HOST_DEVICE`, `_CCCL_COMPILER(...)`, `_CCCL_CUDA_COMPILER(...)`), since they live under CUDA-backend-only code where genuine libcu++ is always available — so ported literally with no wrapper substitution, matching upstream exactly: `thrust/detail/allocator/temporary_allocator.inl` (`defined(_NVHPC_CUDA) || defined(__CUDA_ARCH__)`→`_CCCL_DEVICE_COMPILATION()`), `thrust/system/cuda/detail/core/agent_launcher.h` (`defined(__CUDA_ARCH__) || defined(_NVHPC_CUDA)`→`_CCCL_DEVICE_COMPILATION()`), `thrust/system/cuda/detail/core/util.h` (`__CUDA_ARCH__ >= 600`→`_CCCL_PTX_ARCH() >= 600`), `thrust/system/cuda/detail/malloc_and_free.h` (`ifndef __CUDA_ARCH__`→`_CCCL_HOST_COMPILATION()`).
  - All 4 files staged; `git diff --numstat` = 2/2, 3/3, 1/1, 3/3 respectively, exact match to upstream.
- [X] 1f5498642c12bb6d5ec528e5efc023d50107fb04  Improve defence against the external macros (#4635) [SCOPE: HEADER,TEST,EXAMPLE]
  - Upstream's core change (`.clang-format` `IncludeCategories` rules for `<cuda/std/__cccl/prologue.h>`/`epilogue.h`, `<nv/...>`) and the bulk of the diff (`cub`/`cudax`/`libcudacxx`) are out of scope. Of upstream's thrust-touching files, 15 are in-scope, all pure cosmetic include-block reordering with no semantic change (consequence of the `.clang-format` update), which we ported for byte-level parity with upstream per explicit instruction rather than treating as out-of-scope.
  - 6 of the 15 are deliberate no-ops: `thrust/detail/contiguous_storage.inl`, `thrust/detail/memory_algorithms.h`, `testing/allocator.cu`, `testing/device_delete.cu`, `testing/uninitialized_copy.cu`, `testing/uninitialized_fill.cu` — upstream reorders `<nv/target>` relative to other includes in each, but this HIP port's copy of every one of these files has never had an `<nv/target>` include (confirmed via grep) — nothing to reorder.
  - 2 of the 15 are absent from this fork entirely: `examples/cuda/unwrap_pointer.cu`, `examples/cuda/wrap_pointer.cu` — confirmed via `find` these don't exist anywhere under `projects/rocthrust/`, and `git log --follow` on these paths shows no rocThrust-fork commit ever added them (only upstream/cccl has them) — no-op, nothing to port.
  - Remaining 7 files edited (reordering only, matching upstream's diff exactly): `examples/cuda/explicit_cuda_stream.cu` (`<cuda_runtime.h>` moved after `<iostream>`), `examples/include/timer.h` (`<cuda_runtime_api.h>` moved after `<string>`), `examples/padded_grid_reduction.cu` (`<float.h>` moved before `"include/host_device.h"`), `examples/scan_matrix_by_rows.cu` (`<assert.h>` moved before `"include/host_device.h"`), `examples/sorting_aos_vs_soa.cu` (`<assert.h>` moved before both `"include/host_device.h"`/`"include/timer.h"`), `testing/cuda/cudart.cu` (`<cuda_runtime_api.h>` moved after `<thrust/detail/util/align.h>`), `testing/unittest/cuda/testframework.cu` (`<cuda_runtime.h>` moved after `<numeric>`).
  - All 7 staged; `git diff --numstat` = 2/2, 2/2, 2/1, 2/1, 2/1, 2/2, 2/2 respectively, exact match to upstream's per-file numstat.
- [X] eb7b68775a92b36307db023e6351b6025d6e2d8e c.parallel: reuse CUB agent policies for reduce (#4286) [SCOPE: HEADER]
  - Bulk of upstream diff is `c/parallel`, `cub` (agent policies/ptx-json), and `libcudacxx` infra — out of scope. Only 1 thrust file touched: `thrust/system/cuda/detail/core/triple_chevron_launch.h`, wrapping `doit_host()` in `#if !_CCCL_COMPILER(NVRTC)`/`#endif` (guards a host-only kernel-launch helper from NVRTC-only compilation).
  - Ported literally: this file already uses bare `_CCCL_*` macros directly (matches commit 28's "already migrated" convention), and `#if !_CCCL_COMPILER(NVRTC)` already has direct precedent in sibling files in the same directory (`util.h`, `core/util.h`).
  - Staged; `git diff --numstat` = 7/5, exact match to upstream.
- [X] 1b13308ef2e3849dddcf74ac5db230f480021915 Use list init for test data in iterator docs (#4738) [SCOPE: HEADER]
  - Pure Doxygen doc-comment cleanup, no code semantics: replaces multi-line `vec(N); vec[0]=...;` example snippets with single-line `vec{...}` list-init across 6 iterator headers. Ported literally, matching upstream exactly: `constant_iterator.h`, `counting_iterator.h`, `discard_iterator.h`, `permutation_iterator.h`, `reverse_iterator.h` (3 occurrences), `transform_iterator.h` (3 occurrences).
  - All 6 staged; `git diff --numstat` = 1/5, 1/9, 2/17, 2/15, 3/16, 3/15 respectively, exact match to upstream's per-file numstat.
- [X] a3a00b9164a9c3893aca63dc5186ee72bb98c3ae Make `device_reference<T>::operator=` `const` (#4740) [SCOPE: HEADER,TEST]
  - Real semantic change (proxy-reference const-correctness): all `operator=` overloads on `thrust::detail::reference`/`tagged_reference` and `thrust::device_reference` become `const`-qualified (assigning to a proxy reference doesn't change the reference itself). Ported in full to `thrust/detail/reference.h` and `thrust/device_reference.h`, including the new `const derived_type& derived() const` overload, `const`-qualified `assign_from`/`strip_const_assign_value`, the new explicit `device_reference(const device_reference&) = default;` copy ctor, and the new `device_reference::operator=(const device_reference&) const` overload.
  - The templated `operator=`'s SFINAE was converted upstream from `typename std::enable_if<std::is_convertible<...>>::type` return-type SFINAE to a `::cuda::std::enable_if_t<::cuda::std::is_convertible_v<...>, int> = 0` default-template-parameter form — translated to `_THRUST_STD::enable_if_t`/`_THRUST_STD::is_convertible_v` per this file's internal-implementation-code convention (file already uses `THRUST_HOST_DEVICE`/`_THRUST_STD::` elsewhere, e.g. `_THRUST_STD::move`).
  - `testing/device_reference.cu`: ported the new const-reference test coverage in `TestDeviceReferenceAssignmentFromDeviceReference` verbatim (mixing const/non-const `device_reference`s), plus upstream's incidental list-init cleanup (`device_vector<T0> v0(2, 0)` → `v0{0, 0}`) in the same function.
  - `testing/device_ptr.cu`: **deliberate partial no-op**. Upstream's only change here is two new `#include`s plus two `static_assert(cuda::std::indirectly_writable<...>)`/`static_assert(std::indirectly_writable<...>)` lines proving the new const `operator=` satisfies `std::indirectly_writable`. Checked `thrust/detail/libcxx_wrapper/std/__iterator/concepts.h` — this fork's libcu++ shim only implements a legacy `is_indirectly_readable` trait, no `indirectly_writable` concept exists anywhere in the repo, so the static_assert would not compile. Skipped both the includes (would be unused without the asserts) and the asserts themselves; the underlying const-correctness behavior is still fully ported and tested via `device_reference.cu`.
  - `reference.h`/`device_reference.h`/`device_reference.cu` staged; `git diff --numstat` = 21/23, 9/2, 25/7 respectively, exact match to upstream's per-file numstat. `device_ptr.cu` intentionally left untouched (0/0).
- [X] 80c1a65d438785ae1e75c12925993e7af0b75d9a Add variadic ctor and CTAD to zip_iterator (#4113) [SCOPE: TEST]
  - Test-only commit; only `testing/zip_iterator.cu` touched, no header diff. Confirmed the header-side feature this test exercises (variadic `zip_iterator` ctor, CTAD deduction guide, variadic `make_zip_iterator`) already exists locally in `thrust/iterator/zip_iterator.h` (lines 488, 498, 634, 665-667) from an earlier commit — this is purely new test coverage for already-ported functionality.
  - Ported literally: new "construction from pack" test block in `TestZipIteratorManipulation` plus two new CTAD `static_assert`s, translating `::cuda::std::is_same_v` → `_THRUST_STD::is_same_v` per this file's pre-existing convention (already used at lines 44-49 for `traits::*` static_asserts). Also ported the incidental `;`→`}` typo fix at the end of `TestZipIteratorCopyAoSToSoA`.
  - Staged; `git diff --numstat` = 12/7, exact match to upstream.
- [X] 0c5d8878687f4851516d8e0f013b696368fe7856 Simplify thrust::cuda_cub::swap_ranges (#4182) [SCOPE: HEADER]
  - Ported literally across all 3 touched files. `thrust/detail/reference.h`: two member `swap()` overloads and the free `swap(tagged_reference&, tagged_reference&)` changed from swap-by-reference to swap-by-value (pass-by-value copy-swap-copy idiom, avoids aliasing hazards for proxy references).
  - `thrust/device_reference.h`: `swap(device_reference&)` declaration changed to swap-by-value; the in-class hidden-friend `swap` moved to a free `template <typename T>` function after the class body, preserving upstream's comment that this avoids an nvcc 12.0 miscompile ("incomplete type is not allowed") that hidden friends trigger.
  - `thrust/system/cuda/detail/swap_ranges.h`: simplified from a manual load/swap/store `swap_f` struct (with explicit ctor, `value1_type`/`value2_type` aliases, `using ::cuda::std::swap;`) to a minimal `__swap_f` calling `_THRUST_STD::iter_swap` directly; `it_difference_t` distance computation replaced with `_THRUST_STD::distance`. File is guarded by `#if _CCCL_HAS_CUDA_COMPILER()` (dead code under HIP), consistent with prior local precedent for translating `::cuda::std::X` → `_THRUST_STD::X` in-code while keeping literal `<cuda/std/X>` includes in CUDA-only backend-mirror files.
  - Staged; `git diff --numstat` = 3/3, 12/10, 9/35 for `reference.h`/`device_reference.h`/`swap_ranges.h` respectively, exact match to upstream's per-file numstat.
- [X] 0415f4e0263ddf7074c9dbc226f4b144909681e5 Drop cmake workarounds for nvcc < 12 (#4754) [SCOPE: TEST,EXAMPLE]
  - No-op for 6 of 7 files: `thrust/cmake/ThrustHeaderTesting.cmake` and `thrust/cmake/ThrustUtilities.cmake` (which define `thrust_fix_clang_nvcc_build_for` and the nvcc<11.5 `-Xcudafe=--diag_suppress=186` workaround) don't exist in this fork's independently-written CMake build; `examples/CMakeLists.txt`, `testing/CMakeLists.txt`, `testing/unittest/CMakeLists.txt`, `testing/cuda/stream_per_thread.cmake` exist locally but grep confirmed zero references to `thrust_fix_clang_nvcc_build_for` or the diag_suppress-186 workaround in any of them — these NVCC-specific workarounds were never carried into the fork's build system.
  - `testing/fix_clang_nvcc_11.5.h` existed locally but was a dead orphan (nothing includes/references it anywhere in the tree, confirmed via repo-wide grep) — deleted it via `git rm` to match upstream's deletion and avoid keeping dead code around.
  - Staged (the single deletion); no other files touched.
- [X] 4e7e7cd5cf4878123ea9c2fbaac8515bf74a7a7d Add thrust::transform_n (#4750) [SCOPE: HEADER,TEST]
  - New public API: `transform_n`/`transform_if_n`, 10 overloads (with/without execution policy, 1/2 input ranges, with/without stencil), all thin inline wrappers computing `last = first + count` and forwarding to the existing `transform`/`transform_if`. Ported literally into `thrust/transform.h`, translating `_CCCL_HOST_DEVICE` → `THRUST_HOST_DEVICE`, `detail::execution_policy_base` → `thrust::detail::execution_policy_base` (matches this file's existing qualification style for all other overloads), `::cuda::std::iter_difference_t` → `_THRUST_STD::iter_difference_t`, and `#include <cuda/std/iterator>` → `#include _THRUST_STD_INCLUDE(iterator)` (unconditional form, matching precedent in `thrust/iterator/iterator_traits.h`).
  - Dropped all 10 `_CCCL_NVTX_RANGE_SCOPE(...)` calls from the ported bodies — grep confirmed this macro/NVTX instrumentation doesn't exist anywhere else in the fork, so porting it would either not compile or require unrelated new infrastructure; omitting matches how the fork already omits NVTX tracing added by other upstream commits.
  - `testing/transform.cu`: new `TestTransformN` ported literally (uses `thrust::placeholders`, `thrust::device_vector{...}` list-init — both pre-existing conventions in this test file).
  - Staged; `git diff --numstat` = 160/0 for `transform.h` (170/0 upstream minus the 10 dropped NVTX lines, exact accounting) and 35/0 for `transform.cu`, exact match to upstream.
- [X] 4d67628339d73e7e8faeb3613db3a016322609ab Enable Catch2 tests in Thrust (#2669) [SCOPE: TEST]
  - **DEFERRED, not ported** — this is a real test-infrastructure migration (adds Catch2 framework support to CMake, converts `adjacent_difference.cu`/`alignment.cu`/`binary_search.cu` from the old unittest framework to new `catch2_test_*.cu` files, plus shared `testing/CMakeLists.txt`/`testing/unittest/testframework.cu`/`.h` changes and a new `testing/catch2_test_helper.h`), not a mechanical content translation.
  - Confirmed the diff depends on `cccl_get_catch2()`, a CCCL shared-cmake helper that does not exist anywhere in this repo (grepped the whole monorepo) — rocThrust's `testing/CMakeLists.txt` is independently written and doesn't share CCCL's cmake modules the way upstream's does. Porting this literally would require building new Catch2-fetch/link infrastructure for rocThrust's build, not just translating these 10 files.
  - Decision (confirmed with user): skip and record as an explicit deferred follow-up rather than bundling new build-system infrastructure into this sync pass. No files touched/staged for this commit. **Open question for a future dedicated effort**: whether/how to adopt Catch2 for rocThrust tests (e.g. mirroring the plain `find_package(Catch2 ...)` pattern already used by `shared/origami`/`shared/rocroller` elsewhere in this monorepo, rather than CCCL's `cccl_get_catch2()`).
- [X] ab1ba00183c55b4e61e2ce95faf748718700ebec Allow default-initializing and skipping initialization of Thrust vectors (#4183) [SCOPE: HEADER,TEST]
  - New public API: `default_init_t`/`no_init_t` tag types + `thrust::default_init`/`thrust::no_init` tag objects (`thrust/detail/vector_base.h`); new `vector_base`/`device_vector`/`host_vector` constructors and `resize()` overloads taking these tags; templated `append<SkipInit>()` helper using `if constexpr` to skip `value_initialize_n` when requested.
  - Ported literally across all 5 files, translating `::cuda::std::is_trivially_constructible_v`/`::cuda::std::next` → `_THRUST_STD::` (both already available transitively via this file's existing `thrust/advance.h` include and `_THRUST_STD_INCLUDE(type_traits)`), `_CCCL_HOST` → `THRUST_HOST` in `host_vector.h` (matches this file's existing convention; `device_vector.h`'s new ctors stay untagged, matching its existing untagged-ctor convention). Also ported the incidental `max_size9)` → `max_size()` doc typo fix in `vector_base.h`.
  - `device_vector.h`/`host_vector.h`: new `resize(..., default_init_t)`/`resize(..., no_init_t)` declarations added inside the existing `#if 0` Doxygen-only block (they're implemented in the shared `vector_base` parent).
  - `testing/vector.cu`: new `TestVectorDefaultInitCtor`/`TestVectorNoInitCtor`/`TestVectorDefaultInitResize`/`TestVectorNoInitResize` ported literally.
  - Staged; `git diff --numstat` = 94/0, 36/1, 73/4, 26/0, 26/0 for `vector.cu`/`vector_base.h`/`vector_base.inl`/`device_vector.h`/`host_vector.h` respectively, exact match to upstream's per-file numstat.
- [X] 525ef99f44f572a7956c6a1c8ac49616766780d7 Add `thrust::strided_iterator` and a step for `thrust::counting_iterator` (#4014) [SCOPE: HEADER,TEST]
  - New public API: `thrust::strided_iterator` (new file `thrust/iterator/strided_iterator.h`, wrapping a random-access iterator with a runtime or compile-time stride via `runtime_value<T>`/`compile_time_value<Value>` holders), plus a matching `StrideHolder` template param added to `thrust::counting_iterator` (`thrust/iterator/counting_iterator.h`) so counting iterators can also step by a stride. `thrust::iterator_adaptor` (`thrust/iterator/iterator_adaptor.h`) got 3 mechanical simplifications (dropped redundant `typename iterator_adaptor::` qualification on `reference`/`difference_type` return types now that they're pulled in via `using` in the derived classes) needed to support the new CRTP usage.
  - Ported literally across all 5 upstream-touched files, translating `::cuda::std::X` → `_THRUST_STD::X`, `<cuda/std/X>` → `_THRUST_STD_INCLUDE(X)`, `_CCCL_HOST_DEVICE` → `THRUST_HOST_DEVICE`, `_CCCL_EXEC_CHECK_DISABLE` → `THRUST_EXEC_CHECK_DISABLE`, `_CCCL_DECLSPEC_EMPTY_BASES` → `THRUST_DECLSPEC_EMPTY_BASES`, `_CCCL_COMPILER`/`_CCCL_DIAG_*` → `THRUST_COMPILER`/`THRUST_DIAG_*`.
  - `_CCCL_ASSERT` gap: confirmed (`thrust/detail/libcxx_wrapper/__cccl_config.h`) it's only transitively defined when real libcu++/libhipcxx backs `_THRUST_HAS_DEVICE_SYSTEM_STD`; ported it verbatim in `strided_iterator.h::distance_to()` unguarded, since the file already only fully compiles under real device-std backing.
  - `random_access_iterator` concept gap: guarded the new `static_assert` in `strided_iterator.h` with `#if _THRUST_HAS_DEVICE_SYSTEM_STD || THRUST_STD_VER >= 2020`, matching the exact existing local idiom already used in `thrust/detail/libcxx_wrapper/std/__iterator/concepts.h`, `.../__type_traits/type_identity.h`, and the pre-existing `TestCountingIteratorTraits` in `testing/counting_iterator.cu`.
  - Deliberately dropped upstream's `#include <cuda/std/__algorithm_>` in `testing/counting_iterator.cu` — private/internal libcu++ header, no local precedent, not needed by any new test body (only `std::iota` from `<numeric>` is used), and the fallback `_THRUST_STD_INCLUDE` macro would expand it to a nonexistent bare `<__algorithm_>`.
  - New test file `testing/strided_iterator.cu` ported literally (`TestReadingStridedIterator`, `TestWritingStridedIterator`, `TestWritingStridedIteratorToStructMember`); new tests appended to `testing/counting_iterator.cu` (`TestCountingIteratorDynamicStride`, `TestCountingIteratorStaticStride`, `TestCountingIteratorPointer`).
  - Fork-specific addition beyond upstream's diff: registered the new test via `add_thrust_test("strided_iterator")` in `testing/CMakeLists.txt` (this fork requires explicit per-test registration; no glob-based discovery).
  - Staged; `git diff --numstat`: `iterator_adaptor.h` 3/3 (upstream 6/6, discrepancy not concerning — same 3 targeted edits applied), `counting_iterator.h` 101/19 (upstream 120/22), `testing/counting_iterator.cu` 63/0 (upstream 64/0, due to the dropped include), `testing/CMakeLists.txt` 1/0 (new, fork-only), `strided_iterator.h`/`testing/strided_iterator.cu` new files.
- [X] e1f04c7d907707918233a5bb54c46fbeb5b6f81a Small refactorings in Thrust CUDA (#4764) [SCOPE: HEADER]
  - Pure style/cleanup commit touching only 5 CUDA-only backend files (`thrust/system/cuda/detail/{fill,tabulate,terminate,uninitialized_copy,uninitialized_fill}.h`): dropped explicit functor constructors in favor of aggregate init (`functor{a, b}` instead of `functor(a, b)` + hand-written ctor), removed blank lines after opening namespace braces, collapsed `tabulate.h`'s functor to drop the redundant `Size` template param on the struct (moved to the `operator()` template instead), and collapsed `terminate.h`'s triple-nested namespace to C++17 `namespace system::cuda::detail`.
  - `git apply` on the raw upstream diff silently "Skipped" all 5 hunks (reported success, changed nothing) because the patch's context lines reference pre-translation identifiers (`thrust::distance`) that earlier commits in this sync already converted to `_THRUST_STD::distance` in these same files — hand-edited each file instead, applying the same structural simplification onto the current (already-translated) local content.
  - Per user decision: HIP-side files (`thrust/system/hip/detail/{fill,tabulate,terminate,uninitialized_copy,uninitialized_fill}.h`) have structurally-identical `hip_rocprim` functors but were left untouched — this is a pure style choice, not a functional fix, so it doesn't warrant proactively touching the HIP backend (unlike e.g. the `execution_policy.h` qualification fix mirrored in an earlier commit).
  - Staged; `git diff --numstat` matches upstream exactly per file: `fill.h` 1/10, `tabulate.h` 4/15, `terminate.h` 2/8, `uninitialized_copy.h` 2/14, `uninitialized_fill.h` 2/14.
- [X] 3e2f6e9a6e0436f9d1d2ea8b407dd76d3dc94dc4 Make sure that `cuda` iterators play nicely with the thrust system and traversal machinery (#4806) [SCOPE: HEADER,TEST]
  - Upstream adds `iterator_system`/`iterator_traversal` specializations for `::cuda::discard_iterator`, `::cuda::counting_iterator`, `::cuda::transform_iterator` (from libcu++'s `<cuda/iterator>`) so these libcu++ iterators interoperate with thrust's system/traversal dispatch machinery; also introduces `iterator_system_t` alias usage throughout to simplify `typename iterator_system<X>::type` call sites, and fixes `zip_iterator`'s reference type for iterators with a `void` reference (e.g. `cuda::discard_iterator`).
  - `libcudacxx/include/cuda/__iterator/{discard_iterator,transform_iterator}.h` out of scope (not part of rocThrust).
  - Per user decision ("Port as literal ::cuda:: (CUDA-only)"): ported the new `::cuda::` specializations verbatim rather than assuming libhipcxx parity or skipping. Discovered while implementing that guarding solely with `_THRUST_HAS_DEVICE_SYSTEM_STD` would be unsafe: that macro is `1` under both the libcudacxx (CUDA) *and* libhipcxx (HIP) branches of `thrust/detail/config/libcxx.h`, so a literal `#include <cuda/iterator>` guarded only by it would break real HIP+libhipcxx builds where that header doesn't exist. Used `THRUST_HAS_INCLUDE(<cuda/iterator>)` / `THRUST_HAS_INCLUDE(<cuda/__iterator/discard_iterator.h>)` instead — a self-verifying, backend-safe guard already used elsewhere in this codebase (`libcxx.h`'s own `THRUST_HAS_INCLUDE(<cuda/std/version>)` check).
  - `thrust/iterator/iterator_traits.h`: added a new guarded block (after the existing `iterator_system_t` alias) with the three `::cuda::` specializations, gated behind `THRUST_HAS_INCLUDE(<cuda/iterator>)`. Deliberately left the file's existing unconditional `#include _THRUST_STD_INCLUDE(iterator)` untouched (needed for `it_value_t`/`it_reference_t`/etc.) rather than swapping it for `<cuda/iterator>` as upstream does — this fork's `_THRUST_STD_INCLUDE` abstraction doesn't exist upstream, so the two serve different purposes here and both are kept.
  - `thrust/detail/internal_functional.h`: added `is_discard_proxy<T>` (specialized `true` for `::cuda::discard_iterator::__discard_proxy`, guarded behind `THRUST_HAS_INCLUDE(<cuda/__iterator/discard_iterator.h>)`) and OR'd it into `enable_if_non_const_reference_or_tuple_of_iterator_references`.
  - Mechanical `typename iterator_system<X>::type` → `iterator_system_t<X>` simplification applied to: `thrust/detail/type_traits/pointer_traits.h` (`is_pointer_system_convertible`), `thrust/iterator/constant_iterator.h` and `thrust/iterator/counting_iterator.h` (copy-ctor `enable_if_convertible_t` params), `thrust/iterator/detail/retag.h` (both `retag()` overloads), `thrust/iterator/permutation_iterator.h` (`System1`/`System2` aliases). `retag.h`'s second overload was kept in this fork's existing multi-line parameter style rather than upstream's single-line collapse — cosmetic only, same content.
  - `thrust/iterator/zip_iterator.h`: added `zip_iterator_reference_t<Iter>` helper (falls back to `decltype(*declval<Iter>())` when `it_reference_t<Iter>` is `void`, e.g. for `cuda::discard_iterator`) and used it for the `reference` alias in the `_THRUST_HAS_DEVICE_SYSTEM_STD` branch of `make_zip_iterator_base`. Translated upstream's `_CUDA_VSTD::` → `_THRUST_STD::` per this fork's standing convention; applied unconditionally (not gated behind `THRUST_HAS_INCLUDE`) since it's generic template logic with no hard `::cuda::` type reference.
  - `testing/zip_iterator.cu`: deleted the two dead `#if 0`-guarded test structs (`TestZipIteratorTraversal`/`TestZipIteratorTraversalInstance`, `TestZipIteratorSystem`/`TestZipIteratorSystemInstance`) — pure deletion, no translation needed.
  - Skipped the new `testing/catch2_test_cuda_iterators.cu` (uses `cuda::discard_iterator`/`cuda::counting_iterator`/`cuda::make_transform_iterator` directly in a Catch2 test): consistent with this fork's established precedent that Catch2 test infrastructure is out of scope (`testing/CMakeLists.txt` has no `cccl_get_catch2()`/Catch2 wiring).
  - `git apply` again silently skipped all hunks for the already-translated files (same "Skipped patch" context-mismatch issue as commit 40) — hand-edited each file directly instead.
  - Staged; `git diff --numstat`: `pointer_traits.h` 1/2, `constant_iterator.h` 4/5, `counting_iterator.h` 5/6, `permutation_iterator.h` 2/2, `zip_iterator.h` 8/1, `testing/zip_iterator.cu` 0/127 — all exact matches to upstream. `iterator_traits.h` 37/0 (upstream 31/1) and `internal_functional.h` 17/1 (upstream 11/1) both run higher on the "added" side purely from the extra `THRUST_HAS_INCLUDE` guard/comment lines described above — expected, not an error. `retag.h` 2/2 (upstream 2/4) reflects the kept multi-line style noted above — same content, fewer lines removed.
- [X] c38a6dd5a1d6ad6f3e36bfd745854506312af975 fix cudax's vector_add example that was broken by #4795 (#4814) [SCOPE: HEADER]
  - `examples/cudax/vector_add/vector.cuh` out of scope (cudax is a separate CCCL project, not part of rocThrust).
  - In-scope change is a single missing `#include <thrust/iterator/detail/any_system_tag.h>` added to `thrust/iterator/iterator_traits.h` — fixes a latent missing-include bug for `any_system_tag` (already relied upon transitively before, and now also directly by the `::cuda::` iterator specializations added in the previous commit #4806). Ported verbatim, no translation needed.
  - Staged; `git diff --numstat` = 1/0, exact match to upstream.
- [X] 035da530641bff6c2d78b324294bd3e6c20ab9d6 Refactor Thrust select_system (#4762) [SCOPE: HEADER]
  - Replaces the old per-arity (1-6), macro-style SFINAE `select_system` overload set with a variadic-template + `void_t`-based existence check (`select_system_exists<Tags...>`) and `if constexpr` dispatch; deletes the now-dead `thrust::detail::select_system_fn` callable (`thrust/detail/select_system.h`), the old `.inl` implementation (`thrust/system/detail/generic/select_system.inl`), and the arity-specific `select_systemN_exists` traits (`thrust/system/detail/generic/select_system_exists.h`); trims `enable_if_defined` from `is_metafunction_defined.h` (no longer used); adds `thrust::detail::minimum_system_t` alias to `minimum_system.h`.
  - Confirmed via grep before touching anything: the deleted `thrust/detail/select_system.h` (the callable functor) is not `#include`d or referenced anywhere in this fork already — dead code. All ~40 algorithm `.inl` call sites already invoke `thrust::system::detail::generic::select_system` directly via a `using`-declaration (ADL), not the deleted functor, so no call-site files needed touching.
  - Ported the new `select_system.h` (generic) implementation, translating `_CCCL_HOST_DEVICE`→`THRUST_HOST_DEVICE`, `::cuda::std::void_t/declval/enable_if_t/is_same_v`→`_THRUST_STD::`, `<cuda/std/type_traits>`→`_THRUST_STD_INCLUDE(type_traits)`, and collapsing the 3-level `namespace system { namespace detail { namespace generic` to C++17 `namespace system::detail::generic` (matches the namespace-collapse precedent set in commit e1f04c7d/#4764).
  - Staged; `git diff --numstat`: `is_metafunction_defined.h` 0/6 (exact match), `minimum_system.h` 3/0 (exact match), `select_system.h` (generic) 50/70 (upstream 51/70 — off by one line, cosmetic formatting difference, not a content gap). Deletions: `select_system.h` (detail) 0/72 (exact match), `select_system_exists.h` 0/178 (exact match), `select_system.inl` 0/148 (upstream 0/144 — this fork's pre-existing copy had 4 extra lines from earlier translation-era formatting; irrelevant since the whole file is deleted either way).
- [X] 4d5040efa532a3e9e21bee0ec1d42ed741b3fe48 Port `thrust::strided_iterator` to cuda (#4808) [SCOPE: HEADER,TEST]
  - Almost entirely out of scope: adds a new `::cuda::strided_iterator` type + tests under `libcudacxx/` (not part of rocThrust).
  - In-scope: `thrust/iterator/iterator_traits.h` gets `iterator_system`/`iterator_traversal` specializations for `::cuda::strided_iterator<Iter, Stride>`, added inside the same `THRUST_HAS_INCLUDE(<cuda/iterator>)`-guarded block established in commit 3e2f6e9a/#4806. Ported verbatim.
  - Skipped `thrust/testing/catch2_test_cuda_iterators.cu`'s new `strided_iterator` `TEST_CASE`, consistent with the standing precedent that Catch2 test infrastructure is out of scope for this fork.
  - Staged; `git diff --numstat` = 7/0, exact match to upstream.
- [X] 584fedba37077b0040cccf532229daa2d80ba312 Refactor Thrust internal_functional (#4810) [SCOPE: HEADER,TEST]
  - All 20 touched files are in-scope for rocThrust (no out-of-scope paths this time).
  - Confirmed via grep before touching anything: `tuple_not_binary_predicate` and `compare_first_less_second` (both deleted upstream from `thrust/detail/internal_functional.h`) are unreferenced anywhere else in this fork — safe to delete as dead code.
  - Confirmed `thrust::get`/`thrust::tuple` are aliases for `_THRUST_STD::get`/`_THRUST_STD::tuple` (see `thrust/tuple.h`, `using _THRUST_STD::get;` / `using _THRUST_STD::tuple;`), so translating upstream's `::cuda::std::get<N>(t)` to `_THRUST_STD::get<N>(t)` in touched functor bodies is a semantically-identical mechanical rename, not a behavior change.
  - `thrust/detail/internal_functional.h`: deleted `tuple_not_binary_predicate` (dead) and `compare_first_less_second` (dead, unused two-tuple comparator); converted `is_non_const_reference`, `is_tuple_of_iterator_references` from tag-struct traits to `inline constexpr bool ..._v` variable templates; renamed `enable_if_non_const_reference_or_tuple_of_iterator_references` to `enable_if_assignable_ref` (now an alias template, SFINAE moved from return-type position to a template parameter); dropped hand-written constructors from `predicate_to_integral`, `tuple_binary_predicate`, `unary_transform_functor`, `binary_transform_functor`, `unary_transform_if_functor`, `unary_transform_if_with_stencil_functor`, `binary_transform_if_functor`, `compare_first` (aggregate/brace-init idiom, consistent with commit 40/`e1f04c7d` precedent); kept `equal_to_value`'s ctor per upstream's added comment (needed for nvcc12+clang14 `not_fn_t` copy-ctor workaround); changed `fill_functor`/`uninitialized_fill_functor` copy-ctor/dtor to `= default` (still under `THRUST_EXEC_CHECK_DISABLE`, each with the added "avoid an exec check warning" comment); `operator()(void)` → `operator()()`; body `thrust::get<N>` → `_THRUST_STD::get<N>` throughout. Verified `git diff --numstat` = 40/129, exact match to upstream.
  - `thrust/detail/type_traits.h`: `is_proxy_reference` struct → `inline constexpr bool is_proxy_reference_v = false;` variable template. 1/2, exact match.
  - `thrust/detail/raw_reference_cast.h`: 2 call sites `is_proxy_reference<...>::value` → `is_proxy_reference_v<...>`. 2/2, exact match.
  - `thrust/iterator/{tabulate_output_iterator,transform_input_output_iterator,transform_output_iterator}.h`: each proxy type's `is_proxy_reference<Proxy> : true_type {}` specialization → `inline constexpr bool is_proxy_reference_v<Proxy> = true;`. 1/2, 2/2, 1/2 respectively, all exact matches.
  - `thrust/system/cuda/detail/{count,remove,replace}.h`: `equal_to_value<T>(value)` → `equal_to_value<T>{value}` brace-init at 3 call sites (left the pre-existing `_CCCL_HOST_DEVICE` macro spelling untouched in this subtree, matching this file's established local convention — not part of this commit's scope to retranslate). 1/1 each, exact matches.
  - `thrust/system/detail/generic/{copy,copy_if,fill,merge,mismatch,remove,set_operations,sort,transform,uninitialized_fill}.{inl,h}`: 19 call sites across these 10 files converted from paren-init to brace-init for the functors touched above (`functor_type(args)` → `functor_type{args}`), consistent with the removed explicit constructors. All numstats exact matches to upstream (1/1, 1/1, 2/2, 1/1, 1/1, 2/2, 4/4, 1/1, 5/5, 2/2).
  - `testing/copy.cu`, `testing/raw_reference_cast.cu`: updated the two test-side trait specializations (`is_non_const_reference<only_set_when_expected_it>`, `thrust::detail::is_proxy_reference<std::vector<bool>::reference>`) to the new variable-template form. 1/2 each, exact matches.
  - All 20 files staged.
- [X] 85694c049e5af7b5c0453f272d32f6f74a99f9a0 Refactor `thrust::minimum_type|minimum_system` (#4042) [SCOPE: HEADER,TEST]
  - Ported with a deliberate fork-specific deviation from upstream, confirmed with the user before touching any file. Upstream fully replaces the old 16-arg-capped, `::type`-bearing `minimum_type<Ts...>` struct with a direct variadic alias, and replaces `minimum_system`'s `is_metafunction_defined`-based mechanism with a `minimum_system_impl` SFINAE-overload trick, deleting the old names entirely.
  - Investigation found this fork carries a genuinely-reachable `!_THRUST_HAS_DEVICE_SYSTEM_STD` legacy fallback path (see `thrust/detail/config/libcxx.h:90-96`; only compiled when neither libcudacxx nor libhipcxx is found) inside `zip_iterator.h`'s `#else` branch, which upstream has no equivalent of. That branch uses the bare `minimum_category`/`minimum_system` aliases as metafunction classes (`struct apply : minimum_category<T1, T2>`, relying on the OLD struct's nested `::type`) — applying upstream's deletions verbatim would silently break that build configuration.
  - Resolution (additive, not a straight port): left the OLD 16-arg `minimum_type` struct, `minimum_type_detail::*`, the bare `minimum_category` alias in `zip_iterator.h`, and the old `minimum_system`/`minimum_system_t` in `minimum_system.h` completely untouched (still exercised by the legacy `#else` path and by nothing else). Added a NEW, additive `minimum_type_t` alias template in `thrust/detail/type_traits/minimum_type.h` (own nested `minimum_type_t_detail` namespace to avoid name clashes with the old `minimum_type_detail`) implementing upstream's variadic `smaller_type`/`minimum_type_impl`/`minimum_type_check_marker` recursion under the `_t`-suffixed names. `minimum_system_t` needed no changes at all — it already existed unconditionally in this fork since commit 43 (`035da530`) and was already alias-compatible (no `::type` needed by callers), so it directly serves the call sites upstream simplifies.
  - Call sites updated (only in the modern, unconditionally-compiled-regardless-of-legacy-macro paths, all genuinely safe since `minimum_type_t`/`minimum_system_t` are defined unconditionally):
    - `thrust/detail/overlapped_copy.h`: `typename thrust::detail::minimum_system<System1, System2>::type` → `thrust::detail::minimum_system_t<System1, System2>`.
    - `thrust/iterator/permutation_iterator.h`: same pattern; also dropped the now-genuinely-unused `#include <thrust/iterator/iterator_facade.h>` (verified via grep — no other reference in the file), matching upstream's own cleanup.
    - `thrust/iterator/zip_iterator.h`: in the `#if _THRUST_HAS_DEVICE_SYSTEM_STD` branch only, replaced the `__type_fold_left`/`__type_quote_trait<minimum_system>` / `<minimum_category>` computations with direct `minimum_system_t<iterator_system_t<Its>...>` / `minimum_type_t<iterator_traversal_t<Its>...>`, matching upstream's simplification. Did NOT delete `using minimum_category = minimum_type<Ts...>;` (upstream deletes it, but this fork's `#else` legacy branch at line ~264/283 still needs it) — left it and the entire `#else` branch untouched.
    - `thrust/system/omp/detail/copy.inl`, `thrust/system/tbb/detail/copy.inl` (2 sites each): `typename thrust::detail::minimum_type<traversal1, traversal2>::type` → `thrust::detail::minimum_type_t<traversal1, traversal2>`.
    - `thrust/system/detail/generic/{copy,copy_if,reduce_by_key,transform,unique_by_key}.inl`: removed the now-unused `#include <thrust/iterator/detail/minimum_system.h>` (verified via grep in each file — no other reference to `minimum_system`/`unrelated_systems`), matching upstream; unrelated to the alias-naming decision, safe regardless.
  - Skipped `thrust/testing/catch2_test_minimum_type.cu` (new file) per the established Catch2-out-of-scope precedent (commits 41/44) — confirmed explicitly with the user for this commit too.
  - All 9 touched files verified by reading the resulting diffs in full and staged.
- [X] 9e60fb9187b3c0222d9a875ed1f945005f05cacc Port functional_placeholders_logical Thrust test to Catch2 (#4882) [SCOPE: TEST]
  - **SKIPPED, not ported** — pure Catch2 test-migration commit: deletes the old-framework `testing/functional_placeholders_logical.cu`, adds `testing/catch2_test_functional_placeholders_logical.cu`, and extends the Catch2-only `testing/catch2_test_helper.h` (new `integral_vector_list` type list). This fork has zero Catch2 infrastructure (confirmed via `find` — no `catch2_test_helper.h` or any `catch2_test_*.cu` exist anywhere in `testing/`), consistent with commit 194's (`4d67628339d`) earlier fully-deferred decision to skip Catch2 enablement.
  - Decision (confirmed with user): left `testing/functional_placeholders_logical.cu` in place untouched, still running under this fork's existing unittest framework — no coverage lost. Did not add the new Catch2 file or the helper-file change. No files touched/staged for this commit.
- [X] 5973d44bd59e1c438ccba4c09725df4f023dcf35 Port `thrust::constant_iterator` to cuda (#4812) [SCOPE: HEADER,TEST]
  - Almost entirely libcudacxx-side (new `cuda::constant_iterator` in `libcudacxx/`) — out of scope, that's the separate libcu++ project, not something rocThrust ports.
  - `thrust/iterator/iterator_traits.h`: added `iterator_system`/`iterator_traversal` specializations for `::cuda::constant_iterator<T, Index>`, inserted into the already-established `#if THRUST_HAS_INCLUDE(<cuda/iterator>)` guard block (same guard already covering `::cuda::discard_iterator`/`::cuda::counting_iterator`, per the established CUDA-only-libcu++-feature idiom from earlier commits this sync). 11/11, exact match to upstream. Staged.
  - Skipped the new `TEST_CASE("constant_iterator", ...)` added to `testing/catch2_test_cuda_iterators.cu` — Catch2, out of scope per established precedent (commits 41/44/46).
- [X] 6210162082bb2064e4e76b8632b42708f3e250af Use `cuda::std::type_identity` instead of *identity-like* types (#4893) [SCOPE: HEADER]
  - Upstream replaces every `identity_<T>`/`::cuda::std::identity_<T>`-style helper used purely for its `::type` member with `::cuda::std::type_identity<T>` across 14 rocThrust files, and deletes the now-unused `thrust::detail::identity_` struct definition from `thrust/detail/type_traits.h`.
  - Translation used `::internal::type_identity<T>` (this fork's own pre-existing portability shim at `thrust/detail/libcxx_wrapper/std/__type_traits/type_identity.h`, already used by `counting_iterator.h` prior to this commit, parallel to the existing `::internal::conditional`/`::internal::conjunction` wrappers) instead of a raw `_THRUST_STD::type_identity<T>` — confirmed with the user, since plain `_THRUST_STD::type_identity` would resolve to `::std::type_identity` and fail to compile under `_THRUST_HAS_DEVICE_SYSTEM_STD == 0` + pre-C++20 dialects (`std::type_identity` is C++20-only), a configuration this fork must support that upstream (libcu++-only) doesn't need to.
  - New `#include <thrust/detail/libcxx_wrapper/std/__type_traits/type_identity.h>` added to each of the 13 non-CUDA-backend files (in place of upstream's `#include <cuda/std/type_traits>`).
  - Special case: `thrust/system/cuda/detail/core/util.h` uses direct, unwrapped `::cuda::std::type_identity<AgentPlan>` (not the `::internal::` wrapper) — this file is CUDA-backend-only code that already uses unwrapped `::cuda::std::conditional` elsewhere, so real `::cuda::std::` is always available here; ported upstream's include swap verbatim (`<cuda/std/__type_traits/void_t.h>` → `<cuda/std/type_traits>`).
  - `identity_` struct kept, not deleted, per user decision: a fork-wide grep found 3 usages outside upstream's 14-file diff that still depend on it — `thrust/unique_ptr.h` (6 sites, ROCm-only file that doesn't exist upstream at all), `thrust/iterator/detail/minimum_system.h:45` (this fork still carries an older, un-ported upstream implementation of `minimum_system_impl` that predates upstream's current `decltype`-based SFINAE version — confirmed via `git show <parent>:...minimum_system.h`; unrelated drift, out of scope for this commit), and `thrust/iterator/zip_iterator.h:196` (the `!_THRUST_HAS_DEVICE_SYSTEM_STD` legacy fallback branch preserved since commit 46/`85694c04`). Same "additive, keep old struct" precedent as commit 46.
  - All other call sites across the 14 files were mechanically 1:1 with upstream (no deviations beyond the identity_-struct-retention and CUDA-special-case decisions above). Read all 14 files in full afterward and diffed against upstream's per-file hunks — confirmed byte-for-byte equivalent aside from the `::internal::` vs `::cuda::std::` naming difference.
  - Staged: `thrust/detail/allocator/allocator_traits.h`, `thrust/detail/internal_functional.h`, `thrust/detail/pointer.h`, `thrust/detail/raw_reference_cast.h`, `thrust/detail/temporary_array.h`, `thrust/detail/type_traits.h`, `thrust/detail/type_traits/pointer_traits.h`, `thrust/iterator/constant_iterator.h`, `thrust/iterator/counting_iterator.h`, `thrust/iterator/detail/iterator_adaptor_base.h`, `thrust/iterator/detail/iterator_facade_category.h`, `thrust/random/detail/xor_combine_engine_max.h`, `thrust/random/xor_combine_engine.h`, `thrust/system/cuda/detail/core/util.h`.
- [X] 599d21f090af655d2b0061f3202ed59eaa3517d1 Drop cuda::std::__identity (#4887) [SCOPE: HEADER]
  - Upstream drops libcu++'s internal `::cuda::std::__identity` in favor of the now-public C++20 `::cuda::std::identity` (libcu++-side change in `libcudacxx/include/cuda/std/__functional/identity.h`, out of scope for rocThrust). Only 3 rocThrust call-site files are touched: `thrust/system/cuda/detail/gather.h`, `mismatch.h`, `scatter.h`.
  - Pure mechanical rename, 7 sites total (3 in `gather.h`, 1 in `mismatch.h`, 3 in `scatter.h`): `::cuda::std::__identity{}` → `::cuda::std::identity{}`. All 3 files are CUDA-backend-only (`thrust/system/cuda/detail/`) and already use unwrapped `::cuda::std::` directly, so no `::internal::` wrapper or new include was needed — same convention already established for `system/cuda/detail/core/util.h` in the previous commit (49).
  - `git diff --numstat`: `gather.h` 3/3, `scatter.h` 3/3, exact match to upstream. `mismatch.h` 12/12 in the full diff, but only 1 line (`::cuda::std::__identity{}` → `::cuda::std::identity{}`) is this commit's actual content — the extra churn is pre-existing staged `thrust::distance`/`equal_to` → `_THRUST_STD::` translations from an earlier commit in this sync, not new drift introduced here.
  - All 3 files staged.
- [X] 7371801fb23623fb1f536a23d9749a519fc2de79 Fix RadixEncoder<int> operator() signature for radix sort (#4921) [SCOPE: HEADER,TEST]
  - Upstream fixes `RadixEncoder<int>::operator()` in `thrust/system/detail/sequential/stable_radix_sort.inl`, which had the wrong signature (`unsigned long operator()(long x)`) causing incorrect radix-sort results for `int` on some platforms (CCCL issue #4919).
  - Investigation found this fork's `stable_radix_sort.inl` already has the CORRECT signature (`THRUST_HOST_DEVICE unsigned int operator()(int x)`) — the bug this commit fixes does not exist here. No change made to that file.
  - Ported the new regression test `TestSortTrivial` verbatim to `testing/sort.cu` (translation-free: no `::cuda::std::`/`_CCCL_` symbols in the test body). `git diff --numstat` = 11/0, exact match to upstream.
  - Staged: `testing/sort.cu`.
- [X] 9d5b5c5508cf4ba7348914ed7541d6c7321fe415 Improve RFA PR 2234 (#4888) [SCOPE: HEADER]
  - Almost entirely out of scope: this is a CUB deterministic-reduce (RFA) improvement commit; 6 of 7 touched files are under `cub/` (not part of rocThrust).
  - In-scope: `thrust/type_traits/unwrap_contiguous_iterator.h` — bare `std::declval<Iterator>()` → `::cuda::std::declval<Iterator>()` plus a new `#include <cuda/std/utility>`. Translated per this fork's standing convention: `::cuda::std::declval` → `_THRUST_STD::declval`, `<cuda/std/utility>` → `_THRUST_STD_INCLUDE(utility)`.
  - `git diff --numstat` = 4/1 (upstream 5/2) — cosmetic difference only: this fork's line already needed to wrap to 2 lines under the 120-col limit once `_THRUST_STD::declval` was substituted in, so the net line delta differs slightly from upstream's own wrap; same single logical change.
  - Staged: `thrust/type_traits/unwrap_contiguous_iterator.h`.
- [X] 450622a74f6ca55e6884a02fb179cf9583e06c1a Drop dead code in Thrust reduce (#4969) [SCOPE: HEADER]
  - Upstream deletes a dead ~180-line `doit_step` free function and `reduce` free function inside `namespace __reduce` in `thrust/system/cuda/detail/reduce.h` (CUDA-backend reduce-kernel machinery: `AgentLauncher`/`AgentPlan`/`ReduceAgent`/`DrainAgent`, superseded and no longer called from anywhere in this file).
  - Safety check before deleting: grepped `__reduce::reduce\b|__reduce::doit_step` across `thrust/ testing/ examples/` and found one hit in `thrust/system/hip/detail/reduce.h:149` (`__reduce::reduce(policy, first, num_items, init, binary_op)`). Traced that file's actual namespace nesting (`hip_rocprim { namespace __reduce { ... } // namespace __reduce } // namespace hip_rocprim`, lines 72–189) and confirmed it defines its own separate `hip_rocprim::__reduce::reduce` (backed by `rocprim::reduce`) — an unrelated function that merely shares the `__reduce` namespace label by convention. False alarm; not a real dependency on the CUDA backend's `cuda_cub::__reduce::reduce`/`doit_step` being deleted here.
  - Deleted the block verbatim (confirmed byte-for-byte match to upstream's pre-deletion content first). `git diff --numstat` = 0/180, exact match to upstream's pure deletion.
  - Staged: `thrust/system/cuda/detail/reduce.h`.
- [X] 5e610b0ce8f3175ca01b4211917f9197ed4d97a5 replace `_LIBCUDACXX_HIDE_FROM_ABI` with `_CCCL_API inline` (#4936) [SCOPE: HEADER]
  - Upstream is a repo-wide mechanical rename of the macro `_LIBCUDACXX_HIDE_FROM_ABI` (and `[[nodiscard]] _LIBCUDACXX_HIDE_FROM_ABI`) to `_CCCL_API`/`_CCCL_API inline`, almost entirely in `libcudacxx/`/`cudax/`/`cub/`. Only 3 rocThrust files touched: `thrust/thrust/advance.h`, `thrust/thrust/distance.h`, `thrust/thrust/functional.h`.
  - No-op for this fork: all 3 sites in this fork already have the macro pre-expanded from an earlier porting pass (e.g. `inline THRUST_HOST_DEVICE constexpr void advance(...)`, `THRUST_NODISCARD inline THRUST_HOST_DEVICE constexpr ... distance(...)`, `THRUST_NODISCARD inline THRUST_HOST_DEVICE constexpr auto not_fn(...)`) — the macro name `_LIBCUDACXX_HIDE_FROM_ABI` does not appear anywhere in these files, so there is nothing to rename to `_CCCL_API`.
  - No files changed/staged.
- [X] a0ac7eadf8470ffdd68a4ea858c389e229230252 Port `thrust::tabulate_output_iterator` to `cuda` (#4879) [SCOPE: HEADER,TEST]
  - Upstream adds a new CUDA-only libcu++ header `cuda/__iterator/tabulate_output_iterator.h` (`::cuda::tabulate_output_iterator`, `::cuda::__tabulate_proxy`) plus a new catch2 test suite (out of scope: this fork has no catch2 tests at all).
  - `thrust/detail/internal_functional.h`: added `#include <cuda/__iterator/tabulate_output_iterator.h>` guarded by `THRUST_HAS_INCLUDE` (same CUDA-only-libcu++ convention as the existing `discard_iterator.h` include). Replaced the fork-specific `is_discard_proxy<T>` trait with upstream's more general `is_proxy_reference_v<T>` specialization pattern: kept the existing `is_proxy_reference_v<::cuda::discard_iterator::__discard_proxy>` specialization (now guarded per-include rather than sharing one guard) and added a new `is_proxy_reference_v<::cuda::__tabulate_proxy<Fn, Index>>` specialization guarded by the new include check. Removed `is_discard_proxy` entirely and simplified `enable_if_assignable_ref` accordingly (it already gets discard/tabulate proxy coverage via `is_non_const_reference_v` → `is_proxy_reference_v`). `git diff --numstat` = 16/11 vs upstream 9/9 — extra lines are the fork's per-include `THRUST_HAS_INCLUDE` guards (consistent with how the pre-existing `discard_iterator` specialization was already guarded in this fork), same net logic.
  - `thrust/iterator/iterator_traits.h`: added `iterator_system`/`iterator_traversal` specializations for `::cuda::tabulate_output_iterator<Fn, Index>` (any_system_tag / random_access_traversal_tag) inside the fork's existing `THRUST_HAS_INCLUDE(<cuda/iterator>)`-guarded block, alongside the other CUDA iterator specializations. `git diff --numstat` = 11/0, exact match to upstream.
  - `thrust/iterator/zip_iterator.h`: cosmetic comment-only update ("discard iterator" → "proxy iterators"). `git diff --numstat` = 1/1, exact match to upstream.
  - Staged: `thrust/detail/internal_functional.h`, `thrust/iterator/iterator_traits.h`, `thrust/iterator/zip_iterator.h`.
- [X] 3314a70aa01e477c8f645da810e9810002f80483 Refactor thrust::sequential::sort (#4925) [SCOPE: HEADER]
  - Upstream refactors `thrust/system/detail/sequential/sort.inl` (single file): replaces tag-dispatched struct-based traits (`needs_reverse`, `use_primitive_sort` built from `::cuda::std::is_same`/`_And`/`disjunction`) and 4 separate tag-overloaded `stable_sort`/`stable_sort_by_key` helper functions with `inline constexpr bool` traits plus `if constexpr` branching directly inside 2 unified entry-point functions; also collapses `namespace system { namespace detail { namespace sequential` to `namespace system::detail::sequential`.
  - Translated `::cuda::std::is_same_v`/`greater`/`less` → `_THRUST_STD::is_same_v`/`_THRUST_STD::greater`/`_THRUST_STD::less`, `_CCCL_HOST_DEVICE` → `THRUST_HOST_DEVICE`, `NV_IF_TARGET`/`NV_IS_HOST`/`NV_IS_DEVICE` → this fork's existing `_THRUST_IF_TARGET`/`_THRUST_IS_HOST`/`_THRUST_IS_DEVICE` macros. Deliberately KEPT `::internal::is_arithmetic<KeyType>::value` (rather than switching to `_THRUST_STD::is_arithmetic_v`) for `use_primitive_sort` — this fork's `::internal::is_arithmetic` shim (`thrust/detail/type_traits.h`) works around rocPRIM/HIP arithmetic-type gaps and was already used by the pre-refactor version of this same file; no reason to drop it here. `_CCCL_COMPILER(GCC, <, 10)` kept unwrapped (compiler-detection macro, already used raw elsewhere in this fork, not a libcxx-wrapper concern). Adopted the `namespace system::detail::sequential` nested-namespace syntax (already used elsewhere in this fork, e.g. `thrust/system/cuda/error.h`).
  - `git diff --numstat` = 44/115, exact match to upstream (44 insertions, 115 deletions).
  - Staged: `thrust/system/detail/sequential/sort.inl`.
- [X] 3935bb637c036bac4c817d4a7b369e12aa020e9a Add load-bearing semicolon for MSVC in openMP sort (#5024) [SCOPE: HEADER]
  - Upstream fixes `thrust/system/omp/detail/sort.inl` at 2 locations (in `stable_sort` and `stable_sort_by_key`): updates a stale comment ("MSVC 2015" → "#5020: ... MSVC may yield an error...") and adds a second load-bearing no-op semicolon after the loop-internal `THRUST_PRAGMA_OMP(barrier)` (the fix previously only existed before the loop, not inside it).
  - Already applied in this fork: both locations in both functions already have the exact fixed comment text and the semicolon in all 4 positions (before-loop and in-loop, in both `stable_sort` and `stable_sort_by_key`) — an earlier port already carried this fix. No diff needed.
  - No files changed/staged.
- [X] 6b7ab3b6d8b7445c436b0fe066ec0d9c594041a1 Refactor around `thrust::vector` (#5044) [SCOPE: HEADER]
  - Upstream modernizes 7 files from tag-dispatch/SFINAE-struct patterns to `if constexpr`/`inline constexpr bool` variable templates, as part of a broader `thrust::vector` refactor.
  - `thrust/detail/type_traits/is_thrust_pointer.h`: `struct is_thrust_pointer : is_metafunction_defined<...>` → `inline constexpr bool is_thrust_pointer_v = is_metafunction_defined<...>::value`.
  - `thrust/detail/type_traits/pointer_traits.h`: updated the 2 use-sites (`lazy_is_pointer_convertible`, `lazy_is_void_pointer_system_convertible`) from `is_thrust_pointer<X>::value` to `is_thrust_pointer_v<X>`.
  - `thrust/detail/temporary_array.inl`: removed the `temporary_array_detail::construct_values<T>()` SFINAE-overload-pair and `avoid_initialization` struct; replaced the call site with an inline `if constexpr (!_THRUST_STD::is_trivially_copy_constructible_v<T>) { super_t::value_initialize_n(super_t::begin(), n); }`.
  - `thrust/detail/vector_base.h`/`.inl`: removed the `init_dispatch`/`assign_dispatch` overload-pair declarations and definitions; replaced the two `vector_base(InputIterator, InputIterator[, alloc])` constructor bodies with `static_assert(!::internal::is_integral_v<InputIterator>); // TODO(bgruber): remove, just for testing` + `range_init(first, last);` (this TODO comment is genuinely present in the real upstream commit); replaced `assign(InputIterator, InputIterator)`'s body with `if constexpr (::internal::is_integral_v<InputIterator>) { fill_assign(first, last); } else { range_assign(first, last); }`. Used `::internal::is_integral_v` (this fork's existing shim, already used pre-refactor as `::internal::is_integral`) rather than `::cuda::std::is_integral_v` per this fork's established rocPRIM/HIP arithmetic-type-gap convention. Left the fork-specific extra SFINAE constructor template parameter (`_THRUST_STD::enable_if_t<::internal::is_cpp17_input_iterator<InputIterator>::value, int>`, not present upstream) and `insert`/`insert_dispatch` completely untouched — out of scope for this commit.
  - `thrust/type_traits/is_contiguous_iterator.h`: converted `is_libcxx_wrap_iter`/`is_libstdcxx_normal_iterator`/`is_msvc_contiguous_iterator`/`is_contiguous_iterator_impl` from structs to `inline constexpr bool ..._v` variable templates; moved the public `is_contiguous_iterator`/`is_contiguous_iterator_v` aliases from before the `detail` namespace block to after it (matching upstream's reordering); changed `is_contiguous_iterator` from a struct alias to `_THRUST_STD::bool_constant<detail::is_contiguous_iterator_impl_v<Iterator>>`. Preserved this fork's 3 pre-existing deviations from upstream throughout the refactor: (1) `#if defined(_MSC_VER)` guard (fork) instead of upstream's `_CCCL_COMPILER(MSVC)`, keeping the fork's own comment "Use defined(_MSC_VER) due to Windows CI failures"; (2) `_THRUST_STD::is_pointer_v<::std::_Unwrapped_t<Iterator>>` (translated) instead of upstream's `::cuda::std::is_pointer_v<...>`; (3) the fork-only `#if _THRUST_HAS_DEVICE_SYSTEM_STD || THRUST_STD_VER > 2020` branch (not present upstream at all) choosing between C++20-concept-based `_THRUST_STD::contiguous_iterator<Iterator>` and pre-C++20 `_THRUST_STD::is_pointer<Iterator>::value`, both updated to use `is_thrust_pointer_v<Iterator>` per the renamed trait.
  - `thrust/system/cuda/detail/internal/copy_device_to_device.h` (CUDA-backend-only, `#if _CCCL_HAS_CUDA_COMPILER()`): merged the `device_to_device(..., thrust::detail::true_type)` / `(..., thrust::detail::false_type)` tag-dispatch overloads into the single `device_to_device` entry point using `if constexpr (is_indirectly_trivially_relocatable_to<InputIt, OutputIt>::value)`; added `#include <cuda/std/iterator>`; removed now-unused `#include <thrust/distance.h>` and `#include <thrust/functional.h>`; merged `cudaError status; status = ...` into `const cudaError status = ...` per upstream. Opportunistically normalized the relocated `distance()` call from this fork's `_THRUST_STD::distance` to raw `::cuda::std::distance`, fixing a pre-existing internal inconsistency in this same CUDA-backend-only file (which already used raw `::cuda::std::identity{}` elsewhere) — consistent with this fork's established convention that CUDA-backend-only files use raw `::cuda::std::` throughout, no `_THRUST_STD`/`::internal::` wrapper.
  - `git diff --numstat` per file, all exact matches to upstream except `is_contiguous_iterator.h` (37/66 fork vs 36/65 upstream — the 1-line-per-side excess is attributable to this fork's pre-existing extra `#if _THRUST_HAS_DEVICE_SYSTEM_STD || THRUST_STD_VER > 2020` branch, not a translation error):
    - `is_thrust_pointer.h`: 1/2, exact match.
    - `pointer_traits.h`: 2/2, exact match.
    - `temporary_array.inl`: 4/22, exact match.
    - `vector_base.h`: 0/15, exact match.
    - `vector_base.inl`: 13/43, exact match.
    - `is_contiguous_iterator.h`: 37/66 fork vs 36/65 upstream (explained above).
    - `copy_device_to_device.h`: 22/31, exact match.
  - Staged: `thrust/detail/type_traits/is_thrust_pointer.h`, `thrust/detail/type_traits/pointer_traits.h`, `thrust/detail/temporary_array.inl`, `thrust/detail/vector_base.h`, `thrust/detail/vector_base.inl`, `thrust/type_traits/is_contiguous_iterator.h`, `thrust/system/cuda/detail/internal/copy_device_to_device.h`.
- [X] a6282e9cf4f8d2a20a3328b9473edf7b7cdbe9cd Handle upcoming vector type change (#5036) [SCOPE: HEADER]
  - Upstream touches 32 files (guards against deprecation of `double4`/`[u]long4`/`[u]longlong4`, adds limited support for new `xxx4_[16|32]a` types); only 1 file is in-scope for rocThrust: `thrust/type_traits/is_trivially_relocatable.h`. Upstream's change there wraps the `THRUST_PROCLAIM_TRIVIALLY_RELOCATABLE(long4)`/`ulong4`/`longlong4`/`ulonglong4`/`double4` calls in `_CCCL_SUPPRESS_DEPRECATED_PUSH`/`_CCCL_SUPPRESS_DEPRECATED_POP`, and adds new `long4_16a`/`long4_32a` (and `ulong4`/`longlong4`/`ulonglong4`/`double4` `_16a`/`_32a` variants) guarded by `_CCCL_CTK_AT_LEAST(13, 0)`.
  - Already applied in this fork: `is_trivially_relocatable.h` already contains all of this content verbatim, using this fork's established macro translations (`THRUST_SUPPRESS_DEPRECATED_PUSH`/`THRUST_SUPPRESS_DEPRECATED_POP` and `THRUST_CTK_AT_LEAST`, confirmed as real macros defined in `thrust/detail/libcxx_wrapper/std/__cccl/diagnostic.h` and `cuda_toolkit.h` respectively, wrapping to `_CCCL_SUPPRESS_DEPRECATED_PUSH`/`_CCCL_CTK_AT_LEAST`) — an earlier port already carried this fix. No diff needed.
  - No files changed/staged.
- [X] cd1b8dcbb1c7d679a625ef0def9be54448d81d1d Apply `remove_cvref` in `thrust::is_contiguous_iterator` and refactor all uses (#5050) [SCOPE: HEADER,TEST]
  - Upstream touches 12 files; 3 out of scope (`cub/cub/device/device_for.cuh`, `cub/cub/device/dispatch/tuning/tuning_transform.cuh`, `cudax/test/containers/async_buffer/iterators.cu`); 9 in-scope for rocThrust (5 headers, 4 tests).
  - `is_contiguous_iterator.h`: wrapped the `Iterator` argument to `detail::is_contiguous_iterator_impl_v<...>` in both the `is_contiguous_iterator` alias and the `is_contiguous_iterator_v` variable template with `::internal::remove_cvref_t<Iterator>` (fork translation of upstream's `::cuda::std::remove_cvref_t<Iterator>`, using the pre-existing fork shim in `thrust/detail/type_traits.h`).
  - `is_trivially_relocatable.h`: `is_contiguous_iterator<FromIterator>::value && is_contiguous_iterator<ToIterator>::value` → `is_contiguous_iterator_v<FromIterator> && is_contiguous_iterator_v<ToIterator>` in `is_indirectly_trivially_relocatable_to`.
  - `trivial_sequence.h`: `_trivial_sequence`'s 3rd template parameter changed from `typename is_trivial` to `bool is_trivial`; both specializations changed from `thrust::detail::true_type`/`false_type` to `true`/`false`; `trivial_sequence`'s base-class spec and `super_t` alias changed from `typename thrust::is_contiguous_iterator<Iterator>::type` to `is_contiguous_iterator_v<Iterator>` (unqualified — resolves via enclosing `thrust` namespace, matching upstream's unqualified use).
  - `make_load_iterator.h`: removed the two tag-dispatched `make_load_iterator_impl` overloads; inlined their bodies into `make_load_iterator` via `if constexpr (is_contiguous_iterator_v<It>) { return raw_pointer_cast(&*it); } else { return it; }`. Raw `_CCCL_DEVICE`/`_CCCL_FORCEINLINE` left untouched (established as acceptable in this CUDA-backend-only file).
  - `sort.h`: both `is_contiguous_iterator<X>::value` sites (`ItemsIt`, `KeysIt` in `smart_sort`) → `is_contiguous_iterator_v<X>`.
  - `testing/is_contiguous_iterator.cu`: converted all `thrust::is_contiguous_iterator<X>::value` → `thrust::is_contiguous_iterator_v<X>`; added new `test_is_contiguous_iterator_cvref<T>()` (translating `_CCCL_HOST`→`THRUST_HOST`) with its `DECLARE_GENERIC_UNITTEST` registration.
  - `testing/trivial_sequence.cu`: converted the 2 `ASSERT_EQUAL((bool) ...)` calls to `_v` form, dropping the `(bool)` cast.
  - `testing/type_traits.cu`: converted all `ASSERT_EQUAL((bool) ...)` calls to `_v` form (dropping casts); added the 5 new cv/ref-qualified `device_ptr` checks.
  - `testing/universal_memory.cu`: converted the 1 `static_assert(...::value)` to `_v` form.
  - `git diff --numstat` per file, all exact matches to upstream: `is_contiguous_iterator.h` 3/2, `is_trivially_relocatable.h` 1/1, `trivial_sequence.h` 5/6, `make_load_iterator.h` 8/15, `sort.h` 2/2, `testing/is_contiguous_iterator.cu` 41/24, `testing/trivial_sequence.cu` 2/2, `testing/type_traits.cu` 17/11, `testing/universal_memory.cu` 1/1.
  - Staged: `thrust/type_traits/is_contiguous_iterator.h`, `thrust/type_traits/is_trivially_relocatable.h`, `thrust/detail/trivial_sequence.h`, `thrust/system/cuda/detail/core/make_load_iterator.h`, `thrust/system/cuda/detail/sort.h`, `testing/is_contiguous_iterator.cu`, `testing/trivial_sequence.cu`, `testing/type_traits.cu`, `testing/universal_memory.cu`.
- [X] 71da83a25c2451bf8db7db2ac1e45b63c0742d80 `thrust::cuda::pinned_memory_resource` should dispatch to the host system (#5045) [SCOPE: HEADER,TEST]
  - Upstream touches 7 files, all in-scope. Core change: introduces a new `universal_host_pinned_pointer`/`universal_host_pinned_ptr` alias per backend that uses the *host* system's tag (`cpp::tag`) instead of the device backend's own tag, and switches `cuda::pinned_memory_resource` to use it instead of `universal_pointer`, so CUDA pinned-memory algorithm dispatch prefers the host system (fixes upstream #5027, #1192).
  - `thrust/system/cpp/pointer.h`: shortened `thrust::system::cpp::tag`→`cpp::tag` in the existing `universal_pointer` alias (matching upstream's own in-scope cleanup, already inside `namespace cpp`); added `universal_host_pinned_pointer = universal_pointer<T>;` and its `cpp::` namespace using-alias.
  - `thrust/system/cuda/pointer.h`: reformatted the `universal_pointer` Doxygen block comment to `//!` line-comment style (matching upstream) and added a new `universal_host_pinned_pointer = thrust::pointer<T, cpp::tag, ::cuda::std::add_lvalue_reference_t<T>>;` with matching `//!` doc comment (raw `::cuda::std::`, consistent with this file's established CUDA-backend-only convention); added `using thrust::cuda_cub::universal_host_pinned_pointer;` in both the `system::cuda` and top-level `cuda` namespace blocks.
  - `thrust/system/cuda/memory_resource.h`: `pinned_memory_resource`'s `Pointer` template argument changed from `thrust::cuda::universal_pointer<void>` to `thrust::cuda::universal_host_pinned_pointer<void>`.
  - `thrust/system/omp/pointer.h`, `thrust/system/tbb/pointer.h`: each added `universal_host_pinned_pointer = universal_pointer<T>;` and a namespace using-alias (same pattern as cpp).
  - `thrust/universal_allocator.h`: added `template <typename T> using universal_host_pinned_ptr = thrust::system::__THRUST_DEVICE_SYSTEM_NAMESPACE::universal_host_pinned_pointer<T>;`.
  - `testing/universal_memory.cu`: `TestUniversalHostPinnedThrustVector`'s `static_assert` now checks against `thrust::universal_host_pinned_ptr<T>` instead of `thrust::universal_ptr<T>`, dropping the message string per upstream's reformatting.
  - `git diff --numstat` per file, all exact matches to upstream: `cpp/pointer.h` 5/1, `cuda/memory_resource.h` 1/1, `cuda/pointer.h` 42/22, `omp/pointer.h` 4/0, `tbb/pointer.h` 4/0, `universal_allocator.h` 3/0, `testing/universal_memory.cu` 2/2.
  - Staged: `thrust/system/cpp/pointer.h`, `thrust/system/cuda/pointer.h`, `thrust/system/cuda/memory_resource.h`, `thrust/system/omp/pointer.h`, `thrust/system/tbb/pointer.h`, `thrust/universal_allocator.h`, `testing/universal_memory.cu`.
- [X] 6ccee554169d39f19105d6ef65f68f4e9e220a5e Add vectorized `cub::DeviceTransform` algorithm (#4815) [SCOPE: HEADER]
  - Upstream touches 8 files (mostly `cub/`/`c/parallel/` DeviceTransform internals); only 2 in-scope for rocThrust, both CUDA-backend-only files (no macro translation needed, verbatim port): `thrust/system/cuda/detail/core/util.h`, `thrust/system/cuda/detail/unique.h`.
  - `core/util.h`: refactored `uninitialized_array<T, N>` — replaced `using ref = T[N]` + `enum { SIZE = N }` with `static constexpr ::cuda::std::integral_constant<size_t, N> size{};`; `data()`/`operator[]` now go through a shared `reinterpret_cast<T*>(data_)`; dropped the redundant `int`-indexed `operator[]` overloads (unsigned-only remains); removed `size()` member function, `operator ref&()`, and `get_ref()`, replaced by a single `T (&as_array())[N]` accessor. Verified via grep that no other file under `thrust/system/cuda/` calls `.get_ref()`, `operator ref&`, or `::SIZE` on this type, so nothing else needed changing.
  - `unique.h`: removed the now-dead `using core::detail::uninitialized_array;` inside `consume_tile_impl` (the type is still used fully-qualified elsewhere in the file).
  - `git diff --numstat` per file, both exact matches to upstream: `core/util.h` 12/31, `unique.h` 0/2.
  - Staged: `thrust/system/cuda/detail/core/util.h`, `thrust/system/cuda/detail/unique.h`.
- [X] c16a2e1623e632c986d20ba5715d00f098b77b45 Port `thrust::transform_output_iterator` to `cuda` (#5051) [SCOPE: HEADER,TEST]
  - All 19 libcudacxx-prefixed files (new `cuda/__iterator/transform_output_iterator.h` header, `libcudacxx/include/cuda/iterator`, 14 new libcudacxx tests) out of scope, not part of rocThrust.
  - `thrust/detail/internal_functional.h`: added a new `THRUST_HAS_INCLUDE(<cuda/__iterator/transform_output_iterator.h>)`-guarded include and a matching guarded `is_proxy_reference_v<::cuda::__transform_output_proxy<Iter, Fn>> = true` specialization, placed right after the existing tabulate_output_iterator guarded blocks — same pattern established in commit 41/#4806.
  - `thrust/iterator/iterator_traits.h`: added `iterator_system`/`iterator_traversal` specializations for `::cuda::transform_output_iterator<Iter, Fn>` (delegating to `iterator_system<Iter>`/`iterator_traversal<Iter>`) inside the existing `THRUST_HAS_INCLUDE(<cuda/iterator>)`-guarded block, positioned between the tabulate_output_iterator and transform_iterator specializations to match upstream ordering.
  - Skipped the new `TEST_CASE("transform_output_iterator", ...)` in `thrust/testing/catch2_test_cuda_iterators.cu` — Catch2 test infra remains out of scope per established precedent (commits 41/44/46).
  - Staged; `git diff --numstat`: `iterator_traits.h` 7/0, exact match to upstream. `internal_functional.h` 9/0 (upstream 4/0) — extra lines are the `THRUST_HAS_INCLUDE` guard/`#endif` wrapping this fork adds around the new include and specialization, same expected pattern as commit 41's `internal_functional.h` (17/1 vs upstream 11/1).
- [X] 0788bf61e2b586703f8c839f75c73f64a0fbfdbf Fix `thrust::make_discard_iterator` (#5093) [SCOPE: HEADER]
  - `thrust/iterator/discard_iterator.h`: fixed a real bug where `make_discard_iterator<System>()` always returned `discard_iterator<>` (default System) instead of `discard_iterator<System>`; also dropped the unnecessary `typename discard_iterator<System>::difference_type` dependency in favor of a plain `ptrdiff_t` parameter (always `ptrdiff_t` anyway). Translated `::cuda::std::ptrdiff_t` → `_THRUST_STD::ptrdiff_t` per standard convention; `_CCCL_HOST_DEVICE` already `THRUST_HOST_DEVICE` in this fork.
  - Staged; `git diff --numstat` = 2/3, exact match to upstream.
- [X] 0cf03e41303bb49d3822ec0d2aa4fbc9f9acb489 Port `thrust::permutation_iterator` (#4835) [SCOPE: HEADER,TEST]
  - Of 23 upstream files, all 21 libcudacxx-prefixed files (new `cuda/__iterator/permutation_iterator.h` header + 20 new libcudacxx tests) out of scope, not part of rocThrust.
  - `thrust/iterator/iterator_traits.h`: added `#include <thrust/iterator/detail/minimum_system.h>` (unconditional, needed for `minimum_system_t`, already present in this fork since commit 034/#4762) and, inside the existing `THRUST_HAS_INCLUDE(<cuda/iterator>)`-guarded block (placed between `counting_iterator` and `strided_iterator` per upstream ordering), `iterator_system`/`iterator_traversal` specializations for `::cuda::permutation_iterator<Iter, Offset>` — system computed as `detail::minimum_system_t<iterator_system_t<Iter>, iterator_system_t<Offset>>`, traversal fixed to `random_access_traversal_tag`. No macro/type translation needed — no `_CCCL_*`/`::cuda::std::` tokens in this hunk.
  - Skipped the new `TEST_CASE("permutation_iterator", ...)` in `thrust/testing/catch2_test_cuda_iterators.cu` — Catch2 test infra out of scope per established precedent.
  - Staged; `git diff --numstat` = 12/0, exact match to upstream.
- [X] 11486dea25276ac29b3bee5eeaf8c1d03a186f0d Refactor generic sequence (#5156) [SCOPE: HEADER]
  - Deleted `thrust/system/detail/generic/sequence.inl`, merging its content into `thrust/system/detail/generic/sequence.h`: collapsed the 3 declared overloads (0/1/2 trailing args) into a single defaulted-argument definition (`T init = T{}, T step = T{1}`), replaced the tag-dispatched `compute_sequence_value<T, void>`/`compute_sequence_value<T, enable_if_arithmetic>` specialization pair with a single `if constexpr` branch, and collapsed `namespace system { namespace detail { namespace generic` to C++17 `namespace system::detail::generic` (precedent already established in this fork's `select_system.h`).
  - Translated `::cuda::std::is_arithmetic_v<T>` → `::internal::is_arithmetic<T>::value` (this fork's existing `internal::` wrapper only exposes the struct form `is_arithmetic`, not an `_v` variant); `::cuda::std::move` → `_THRUST_STD::move`; `_CCCL_HOST_DEVICE`/`_CCCL_EXEC_CHECK_DISABLE` → `THRUST_HOST_DEVICE`/`THRUST_EXEC_CHECK_DISABLE` per standard convention.
  - Confirmed no call-site changes needed: `thrust/detail/sequence.inl`'s 3 call sites use `using thrust::system::detail::generic::sequence;` + ADL calls with varying arg counts, all resolving against the new single defaulted-argument overload.
  - Staged; `git diff --numstat`: `sequence.h` 34/19, `sequence.inl` 0/95 (deleted) — both exact matches to upstream.
- [X] e703c48ced0b5ba23daf4bb8d87eb328e483ed72 Update doc errors set_operations.h (#5177) [SCOPE: HEADER]
  - Pure documentation typo fix: `<tt>[first2, last1)</tt>` → `<tt>[first2, last2)</tt>` in 16 Doxygen comments across `set_difference`/`set_symmetric_difference`/`set_union` overloads. No code/logic change, no macro translation. Applied via `replace_all`, matching upstream exactly.
  - Staged; `git diff --numstat` = 16/16, exact match to upstream.
- [X] 9e4cd939db12d7d7ae8027c0df5bf2f89a3c8851 [pre-commit.ci] pre-commit autoupdate (#4365) [SCOPE: HEADER,TEST]
  - Clang-format v19→v20 repo-wide reformat; of 149 upstream files, only 7 touch rocThrust (rest are cub/, libcudacxx/, cudax/, c2h/, benchmarks/, docs/, .github/ — out of scope).
  - Purely cosmetic: clang-format v20 now inserts a space before the call-parens in the parenthesized-callee-to-avoid-macro-expansion idiom, e.g. `(std::max)(a, b)` → `(std::max) (a, b)`. Applied verbatim to `testing/shuffle.cu` (1), `thrust/mr/disjoint_pool.h` (2), `thrust/mr/pool.h` (3), `thrust/system/cuda/detail/find.h` (2, this file's already-established raw `::cuda::std::min` since it's CUDA-backend-only), `thrust/system/detail/generic/find.inl` (2, applied to this fork's already-translated `_THRUST_STD::min` token instead of upstream's raw `::cuda::std::min`), `thrust/system/tbb/detail/reduce_intervals.h` (1, this file still uses raw `::cuda::std::min` in this fork).
  - `thrust/complex.h`: 3x doc typo fix `denomimator` → `denominator`.
  - Staged; `git diff --numstat` all 7 files match upstream exactly: `shuffle.cu` 1/1, `complex.h` 3/3, `disjoint_pool.h` 2/2, `pool.h` 3/3, `find.h` 2/2, `find.inl` 2/2, `reduce_intervals.h` 1/1.
- [X] 74dac60f7db268d2a221e42e218c283d9ef3ce34 Refactor thrust cuda replace (#5180) [SCOPE: HEADER]
  - `thrust/system/cuda/detail/replace.h` (CUDA-backend-only, raw macros): removed the hand-written constructors from `__replace::constant_f<T>` and `__replace::new_value_if_f<Predicate, NewType, OutputType>` in favor of aggregate/brace-init (consistent with the aggregate-init idiom precedent used elsewhere in this fork, e.g. commit 40/44's functor refactors); converted all 5 call sites from parenthesized-ctor calls to brace-init (`constant_f<T>(new_value)` → `constant_f<T>{new_value}` ×3, `new_value_if_t(predicate, new_value)` → `new_value_if_t{predicate, new_value}` ×2); dropped the trailing `// struct X` end-of-struct comments to match upstream.
  - No macro translation needed — file already uses this fork's established CUDA-backend-only raw-macro convention, matching upstream's before-state exactly.
  - Staged; `git diff --numstat` = 7/19, exact match to upstream.
- [X] 77eb9a6414c62799914c8983455a2cb2897309a1 Implement transform[_if]_n in the generic system (#5182) [SCOPE: HEADER]
  - Deleted `thrust/detail/transform.inl` (240 lines) and `thrust/system/detail/generic/transform.inl` (189 lines); their bodies moved inline into `thrust/transform.h` and `thrust/system/detail/generic/transform.h` respectively, matching upstream's restructure.
  - `thrust/system/detail/generic/transform.h`: added forward declarations of the 5 `thrust::transform`/`transform_if` exec-policy overloads (with the "we cannot #include <thrust/transform.h>" comment, since that header now includes this one); collapsed `namespace system { namespace detail { namespace generic {` → `namespace system::detail::generic` (matches upstream's own change here, consistent with this fork's established namespace-collapse precedent); filled in the 5 pre-existing declarations with the zip-iterator/`for_each`-based bodies (unchanged logic, just relocated from the deleted `.inl`); added 10 new `transform_n`/`transform_if_n` generic-system overloads (policy + no-policy pairs for unary/binary transform_n and unary/unary+stencil/binary+stencil transform_if_n), each simply forwarding to the corresponding range-based `thrust::transform`/`transform_if` via `first + count`.
  - `thrust/transform.h`: added includes `iterator/iterator_traits.h`, `system/detail/adl/transform.h`, `system/detail/generic/select_system.h`, `system/detail/generic/transform.h`, and a self-include of `<thrust/transform.h>` (matches upstream literally; harmless under `#pragma once`, inherited from the merged-in `.inl`'s own include list); converted the 5 exec-policy declaration-only overloads into definitions (`THRUST_EXEC_CHECK_DISABLE` + dispatch to `system::detail::generic::transform`/`transform_if`) and the 5 no-policy declaration-only overloads into definitions (`select_system` over each iterator's `iterator_system<>::type`, then re-dispatch through the exec-policy overload); updated all 10 existing inline `_n` overloads to dispatch through the new generic `transform_n`/`transform_if_n` (policy) or `select_system` + `thrust::transform_n`/`transform_if_n` (no-policy) instead of degrading straight to `first + count` ranges — this also fixes a real pre-existing bug where the `transform_if_n` policy/no-policy bodies called `thrust::transform(...)` instead of `thrust::transform_if(...)`; removed the trailing `#include <thrust/detail/transform.inl>` (file deleted).
  - Deliberately omitted every `_CCCL_NVTX_RANGE_SCOPE(...)` call upstream adds in this commit: `NVTX_RANGE_SCOPE` instrumentation doesn't exist anywhere else in this fork (confirmed via repo-wide grep — never ported), so it's out of scope here too, consistent with dropping other CUDA-profiling-only additions in prior commits.
  - Staged; `git diff --numstat`: `system/detail/generic/transform.h` = 301/15 (exact match to upstream), `transform.h` = 157/25 (upstream is 167/25; the 10-line shortfall is exactly the 10 omitted NVTX lines — removed-line count matches exactly), `detail/transform.inl` = 0/240 deleted (upstream 0/250; fork's pre-existing translated file was already shorter), `system/detail/generic/transform.inl` = 0/189 deleted (upstream 0/185; same reason).
- [X] 961efabbc7a60b892d93e9c91af5fdddc1f1adaa Replace cuda version checks with `_CCCL_CTK_XXX()` macro (#5204) [SCOPE: HEADER,TEST]
  - Despite the title, the thrust-relevant hunks are plain deletions of dead code guarded by obsolete `CUDART_VERSION` checks (< 5000 / == 3000 / >= 4020), not macro replacements — no `_CCCL_CTK_XXX()` call actually appears in any thrust file touched by this commit.
  - `thrust/detail/malloc_and_free.h`: deleted the `nvbug 992955` WAR (`#if THRUST_HAS_CUDA_COMPILER() / #if CUDART_VERSION < 5000` spoofed `free(int* volatile)`), text matched upstream's pre-commit state exactly. numstat: 0/15, exact match to upstream's 0/15.
  - `thrust/system/cuda/error.h`: this is a vestigial, unbuilt CUDA-parity file (nothing in the fork's headers/tests/build includes it except its own `detail/error.inl` and `memory_resource.h`) kept structurally in sync with upstream's `cuda/error.h`. Applied the same change: removed the `#if CUDART_VERSION >= 4020` / `>= 5000` guards around enum values (all values now unconditional) and re-aligned the `=` columns to match upstream's new formatting. numstat: 49/56, exact match to upstream's 49/56.
  - `thrust/system/hip/error.h` (the file actually built): NOT touched — it already has no `CUDART_VERSION`-style guards; all enum values were already unconditional from an earlier port. No action needed.
  - `thrust/testing/vector.cu`: deliberately NOT ported. Upstream deletes a try/catch testing `resize`/`reserve(SIZE_MAX)` exception behavior, guarded by `#if _CCCL_CUDA_COMPILATION() && CUDART_VERSION == 3000` — dead code upstream (no supported CUDA is version 3000). In the fork, an earlier rocThrust-specific cleanup (commit `43f3d7ff5073`, "Check and remove deprecations") already stripped that guard, so the equivalent try/catch (with `hipGetLastError()` in the catch) currently runs unconditionally on every HIP test build. Deleting it to mirror upstream would silently drop active HIP test coverage that upstream's stated rationale doesn't apply to — confirmed with the user, left untouched. numstat: 0/0 (no change), vs. upstream's 0/33.
- [X] a632178e9977b4d1e75b2a18dba239e7723451e8 Migrate Thrust transform tests to Catch2 (#5197) [SCOPE: TEST] — OUT OF SCOPE, no changes
  - Pure Catch2 test-framework migration: adds `thrust/testing/catch2_test_transform.cu`, deletes `thrust/testing/transform.cu`, touches `catch2_test_helper.h` only to register the new file. No functional/header changes.
  - The fork has no Catch2 test infrastructure at all (confirmed: no `catch2_test_helper.h` anywhere in the tree) and still runs the legacy `testing/transform.cu`. Same out-of-scope category as commits 41/44/46. No files touched.
- [X] 9b34fe9249cbb1b308d4d2b9d99faacc4cf86983 Implement `thrust::swap_ranges` via `transform` in CUDA system (#5243) [SCOPE: HEADER]
  - The only thrust-relevant file touched upstream, `thrust/system/cuda/detail/swap_ranges.h`, is a vestigial, `_CCCL_HAS_CUDA_COMPILER()`-guarded parity file in this fork (never compiled for HIP — same category as `cuda/error.h` handled in commit 961efabbc7 earlier). Mirrored upstream's rewrite there for structural parity: added an `if constexpr (is_indirectly_trivially_relocate_to_v<...>)` fast path that swaps via a zip-iterator `transform` call (`__swap_f` returning `tuple{t, u}`) and falls back to the renamed `__swap_fallback_f` index-based `parallel_for` path otherwise.
  - Confirmed with the user this fast path is NOT also ported into the real, compiled `thrust/system/hip/detail/swap_ranges.h` — that file was not touched by this upstream commit and has already diverged structurally (different `__swap_ranges::swap_f` namespace/functor layout); porting the optimization there would be a new feature addition, not a 1:1 sync, and was explicitly declined.
  - Translation: kept `_CCCL_HOST_DEVICE` untranslated (this file's established, pre-existing convention — never translated to `THRUST_HOST_DEVICE` even before this commit); `::cuda::std::X` → `_THRUST_STD::X` (matches this file's pre-existing usage); `::cuda::proclaim_copyable_arguments` → `::internal::proclaim_copyable_arguments` (confirmed via `testing/address_stability.cu` as this fork's established mapping for the same libcxx-wrapper facility).
  - `thrust/benchmarks/bench/swap_ranges/basic.cu` (new file upstream): out of scope, fork has no `benchmarks/` directory.
  - numstat: 35/8 vs. upstream's 31/8 — the 4-line difference is a deliberate `clang-format`-driven wrap of the `cuda_cub::transform(...)` call's argument list, since `::internal::proclaim_copyable_arguments` is longer than `::cuda::proclaim_copyable_arguments` and pushed the single-line form past this repo's 120-column limit.
- [X] 33900a1fb128269148bfa78edd8d53d4ca3557b4 Add a macro to disable PDL (#5316) (#5329) [SCOPE: HEADER] — ALREADY APPLIED, no changes
  - Only thrust-relevant file: `thrust/system/cuda/detail/core/triple_chevron_launch.h`, which changes `#if _CCCL_HAS_PDL` → `#if _CCCL_HAS_PDL()` (parenthesized macro invocation).
  - Diffed the fork's current file directly against upstream's exact post-commit content (`git show 33900a1f...:thrust/thrust/system/cuda/detail/core/triple_chevron_launch.h`): already identical except for the expected `NV_IF_TARGET` → `_THRUST_IF_TARGET` macro translation. The fork's baseline already carries the parenthesized `_CCCL_HAS_PDL()` form. No edit needed.
- [X] 7296b79e16f148cd2c8c3114971d9a9ecbb59372 [Backport branch/3.1.x] PR #5396 and #5566 (#5611) [SCOPE: HEADER,TEST] — OUT OF SCOPE, no changes
  - `cub/cub/agent/agent_select_if.cuh`: pure CUB internals (`cub::DispatchSelectIf`, `cub::SelectImpl::SelectPotentiallyInPlace`, `cub::NullType`, `guarded_inequality_op`), entirely outside rocThrust's scope.
  - `thrust/system/cuda/detail/unique.h` (~571-line rewrite) and `thrust/testing/cuda/unique.cu` (~112-line new test): both confirmed vestigial/unbuilt in this fork — the header is fully wrapped in `#if _CCCL_HAS_CUDA_COMPILER()` (always false for HIP builds) and the test has no CMake target (`testing/CMakeLists.txt` only registers `add_thrust_test("unique")` / `add_thrust_test("unique_by_key")`, no `cuda/unique` entry).
  - `thrust/system/hip/detail/unique.h` (the real, compiled path) already dispatches directly to `rocprim::unique(...)`, architecturally equivalent to what this upstream commit achieves for CUDA via `cub::DispatchSelectIf` — no behavioral gap to close.
  - Confirmed with the user: skip porting entirely given the ~680 combined lines sit entirely inside never-built code with zero impact on the real backend. No files touched.
- [X] ef834f59525855d9b63c826cb7263c51028538e5 Fixes `thrust::unique` for non-const `equality_op` (#5652) (#5656) [SCOPE: TEST] — OUT OF SCOPE, no changes
  - `cub/cub/agent/agent_select_if.cuh`: pure CUB fix (makes `guarded_inequality_op` invocable with a mutable call operator), entirely outside rocThrust's scope.
  - `thrust/testing/cuda/unique.cu`: adds `TestUniqueWithCustomEqualityOpMutable`, a new test for the fix above. This is the same vestigial test file confirmed earlier this session (commit 7296b79e) to have no CMake test target (`testing/CMakeLists.txt` registers no `cuda/unique` entry) — the test would never build or run in this fork.
  - Confirmed with the user: skip both files, no changes made.
- [X] 7edde877f8f4b15c55568beb973a7654e48ba06b Fix `thrust::malloc` for `void` (#5698) (#5718) [SCOPE: HEADER]
  - Real bug fix in an actively-built file: `thrust::malloc<T>` used `sizeof(T) * n` unconditionally, which is ill-formed for `T = void`. Fixed by splitting into `if constexpr (is_void_v<T>) { allocate n bytes directly } else { allocate sizeof(T) * n bytes }`, exactly mirroring upstream's rewrite of `thrust/system/detail/generic/memory.inl`.
  - Translation: added `#include _THRUST_STD_INCLUDE(__type_traits/is_void.h)` (same granular-include pattern already used elsewhere, e.g. `extrema.h`'s `_THRUST_STD_INCLUDE(__algorithm/max.h)`); `::cuda::std::is_void_v` → `_THRUST_STD::is_void_v` (confirmed existing fork usage in `detail/type_traits/iterator/is_output_iterator.h`); kept the file's existing `THRUST_HOST_DEVICE` (already translated form, no `_CCCL_HOST_DEVICE` present).
  - numstat: 11/2, exact match to upstream's 11/2. Staged.
- [X] 62b14d19b77cfc5b549c97177ea80030e25ba5ec [BACKPORT 3.1] Use forward declarations of extended floating point types instead of including the headers (#5846) (#5978) [SCOPE: HEADER]
  - Only thrust-relevant file: `thrust/system/cuda/detail/sort.h`, a vestigial `_CCCL_HAS_CUDA_COMPILER()`-guarded parity file (same category as `error.h`/`swap_ranges.h`/`cuda/detail/sort.h` handled earlier this session). Mirrored the deletion of the direct `cuda_fp16.h`/`cuda_bf16.h` includes (now unneeded since CUB/CCCL forward-declares those types elsewhere) for structural parity.
  - All other files (`cub/cub/thread/thread_reduce.cuh`, `cub/cub/thread/thread_simd.cuh`, various `libcudacxx`/`cuda/std` headers, `python/cuda_cccl/pyproject.toml`) are pure CUB/libcu++/tooling, out of rocThrust's scope.
  - numstat: 0/11, exact match to upstream's 0/11. Staged.
- [X] 5fefb30bc8e597a288352372e9786496bb2f733f [Backport 3.1]  Backport iterator fixes (#5977) [SCOPE: HEADER,TEST] — BLOCKED/OUT OF SCOPE, no changes
  - Large libcudacxx iterator refactor (89 lines in `libcudacxx/include/cuda/std/__iterator/iterator_traits.h` alone plus many `cuda/__iterator/*.h` and `cuda/experimental` files) renames two core traits: `__is_cpp17_input_iterator<T>::value` → `__has_input_traversal<T>` (struct → bool variable) and `__is_cpp17_random_access_iterator<T>::value` → `__has_random_access_traversal<T>`.
  - This fork consumes both through a live wrapper, `thrust/detail/libcxx_wrapper/std/__iterator/iterator_traits.h`, which aliases directly to `_THRUST_STD::__is_cpp17_*` when `_THRUST_HAS_DEVICE_SYSTEM_STD` (confirmed `=1` for real HIP builds via `thrust/detail/config/libcxx.h`). Porting would require updating that wrapper plus removing `::value` at ~15 call sites (`vector_base.h`/`.inl` ×4, 11 `testing/*.cu` files).
  - Whether the fork's actual libhipcxx dependency (an external repo, not present anywhere in this tree — confirmed zero hits for either trait name outside rocthrust) has already picked up this very recent (2025-09-24) upstream rename is unverifiable from here. Porting blind risks breaking real HIP builds if it hasn't.
  - Also includes a lower-risk-looking but same-dependency-uncertainty piece: `thrust/iterator/iterator_traits.h` swaps `cuda::transform_iterator<Iter, Fn>` → `<Fn, Iter>` template param order (guarded by `THRUST_HAS_INCLUDE(<cuda/iterator>)`) — deferred for the same reason.
  - Confirmed with the user: skip entirely, no files touched. Should be revisited once the fork's libhipcxx pin is confirmed to include this rename.
- [X] ecce794c363cf65c97e59faf5caf376abf8da340 [Backport to 3.1] Fix dereferencing nullptr in thrust::device_reference (#4226) (#6002) [SCOPE: HEADER,TEST]
  - Real bug fix: `thrust::detail::reference`'s `swap`/`operator value_type`/`assign_from` previously dispatched via a null `System*` pointer (assuming `get_value`/etc. never touch system state) — unsafe if a system is stateful. Fixed by dispatching through a real, default-constructed `tag_type` object (`iterator_system_t<pointer>::tag_type`) taken by address instead.
  - Notably, `thrust/system/hip/detail/execution_policy.h` already had the `tag_type` member this fix relies on (a pre-existing rocThrust addition, predating this sync), so the HIP backend needed no change — only the host backends (`cpp`/`omp`/`tbb`/`sequential`) needed `tag_type` added to match.
  - `thrust/detail/reference.h`: `swap`/`operator value_type`/`assign_from` rewritten to use `typename iterator_system_t<pointer>::tag_type tag;` + `&tag` in place of null pointers, per upstream. numstat 10/16, exact match.
  - `thrust/detail/seq.h`: comment-only update. numstat 1/1, exact match.
  - `thrust/iterator/detail/minimum_system.h`: added `is_unrelated_systems<System>` trait (false, specialized true for `unrelated_systems<Ts...>`) — works even though this fork's `minimum_system` resolution mechanism (`::internal::If<is_metafunction_defined<...>>`) already differs structurally from upstream's SFINAE `minimum_system_impl` overloads, since the trait only inspects the `unrelated_systems<Ts...>` marker type itself. numstat 6/1, exact match.
  - `thrust/system/detail/generic/select_system.h`: extended the binary `select_system` overload's `else` branch into a 3-way `if constexpr`/`else if constexpr`/`else` + trailing `THRUST_UNREACHABLE()` (translated from `_CCCL_UNREACHABLE()`, confirmed pre-existing in `libcxx_wrapper/std/__cccl/unreachable.h`), improving diagnostics when systems are unrelated vs. a genuine internal bug. numstat 10/2, exact match.
  - `thrust/system/cpp/detail/execution_policy.h`, `thrust/system/omp/detail/execution_policy.h`, `thrust/system/tbb/detail/execution_policy.h`: added `using tag_type = tag;` to each `execution_policy<tag>` specialization. numstat 3/1 each, exact match.
  - `thrust/system/detail/sequential/execution_policy.h`: added `tag_type` to both the `execution_policy<tag>` specialization and the generic `execution_policy<Derived>` template; changed `tag`'s default constructor to `= default` and added a new templated converting constructor allowing any `execution_policy<DerivedPolicy>` to convert to the sequential `tag` (needed so `minimum_system` can pick the sequential tag as a stateless fallback). numstat 11/2, exact match.
  - `thrust/testing/catch2_test_minimum_system.cu` (new file upstream): out of scope, Catch2 test infrastructure doesn't exist in this fork (same category as commits 41/44/46/a632178e).
  - All 8 real files staged.
- [X] 30ae2aca9d54c51262f06e5ebce598562ceb884b [Backport 3.1]: [CUB] Replace several direct uses of `__clz` (#6099) (#6202) [SCOPE: HEADER] — SKIPPED, no changes
  - Only thrust-relevant file: `thrust/detail/integer_math.h`. All other touched files (`cub/cub/block/block_radix_rank.cuh`, `cub/cub/warp/specializations/*.cuh`, `libcudacxx/include/cuda/std/__bit/*.h`) are pure CUB/libcudacxx, out of scope.
  - Not a thrust bug fix — CUB modernizing to reuse a newly-available C++20 `<bit>` function; the thrust-side change (delete hand-rolled `clz()`, rewrite `log2()` to call `::cuda::std::countl_zero(::cuda::std::__to_unsigned_like(x))`) just rides along.
  - `countl_zero` is a stable, long-standing C++20 stdlib function, but `__to_unsigned_like` is a private/internal libcu++ helper; neither symbol is used anywhere else in this fork, so whether the fork's pinned libhipcxx provides both is unverifiable from this repo — same class of risk as commit 5fefb30bc8's skipped iterator-trait rename.
  - The existing hand-rolled `clz()` (device `::__clz` intrinsic + host bit-scan-loop fallback via `_THRUST_IF_TARGET`) is self-contained and already correct. Confirmed with the user: skip, keep `clz()` as-is, no files touched.
- [X] 1128b6f7971751f181906692b72841169b4eff1d Bump branch/3.1.x to 3.1.1. (#6235) [SCOPE: HEADER] — OUT OF SCOPE, no changes
  - Pure version-bump commit (CCCL 3.1.0 → 3.1.1): CMake config-version files, `cub/version.cuh`, `libcudacxx/.../version.h`, and `thrust/version.h`'s `THRUST_VERSION` macro (300100 → 300101).
  - Fork's `THRUST_VERSION` is currently `300003` (3.0.3) — rocThrust maintains its own independent version number tied to ROCm release cadence, not CCCL's upstream patch-bump numbering. Confirmed with the user: no changes.
- [X] 471db1dc608ec56753685373217d0bf8728424dd Always include `<new>` when we need operator new for clang-cuda (#6310) (#6445) [SCOPE: HEADER] — PORTED
  - `thrust/detail/internal_functional.h` (real, generic) and `thrust/system/detail/generic/uninitialized_copy.inl` (real, generic): added a new guarded include `#if THRUST_HAS_INCLUDE(<cuda/std/__new/device_new.h>) / #include <cuda/std/__new/device_new.h> / #endif`, matching the exact 3-include guarded-and-commented pattern already established in `internal_functional.h` for CUDA-only libcu++ headers with no confirmed libhipcxx counterpart, rather than upstream's bare unconditional include. `uninitialized_copy.inl` also had a cosmetic blank-line removal before the placement-new call, mirrored exactly.
  - `thrust/system/cuda/detail/uninitialized_copy.h` and `uninitialized_fill.h` (vestigial parity files, confirmed via `_CCCL_HAS_CUDA_COMPILER()` guard): mirrored upstream exactly — added the unconditional include (already inside the file's outer CUDA-compiler guard) and removed the `#if _CCCL_CUDA_COMPILER(CLANG) ... out = in;/out = value; ... #else ... ::new (...) ... #endif` workaround, keeping only the unconditional placement-new call. numstat 2/6 for each, exact match to upstream.
  - Confirmed the real, built `thrust/system/hip/detail/uninitialized_copy.h` and `uninitialized_fill.h` never had an equivalent clang-cuda-style workaround — they already use unconditional placement new successfully (annotated `THRUST_HIP_DEVICE_FUNCTION`), so no HIP-side bug exists and no change was needed there.
  - All 4 files staged.
- [X] 17d17d4364c566fe91ab9b266b9de46e5dbc7eb8 Fix offset_iterator tests (#6436) (#6446) [SCOPE: TEST] — PORTED
  - Only file: `testing/cuda/offset_iterator.cu`. Vestigial (no CMake test target, same as `testing/cuda/mismatch.cu`/`sort.cu`/`cudart.cu` handled earlier), but mirrored for parity per this session's established precedent for edited (not new) vestigial `testing/cuda/*.cu` files.
  - Fixed `device_only_iterator::operator-`'s return type from `bool` to `difference_type`, and added the missing `operator+` friend function. numstat 6/1, exact match to upstream. Staged.
- [X] 2f940a0c57d38d21e245d6cfb496a014b95235a1 Bump branch/3.1.x to 3.1.2. (#6433) [SCOPE: HEADER] — OUT OF SCOPE, no changes
  - Pure version-bump commit (CCCL 3.1.1 → 3.1.2), same category as `1128b6f797`. Fork's independent `THRUST_VERSION` (300003) is not synced to CCCL's upstream patch-bump numbering. Confirmed with the user: no changes.
- [X] 8504f319d3d8472602a24f4931727502047de393 Ensure that `detect_wrong_difference` is a valid output iterator (#6450) (#6453) [SCOPE: TEST] — PORTED
  - Only file: `testing/cpp/adjacent_difference.cu`. Vestigial (no CMake test target), mirrored for parity per this session's established precedent.
  - Added the 5 missing iterator-trait typedefs (`difference_type=void`, `value_type=long long`, `pointer=void`, `reference=detect_wrong_difference`, `iterator_category=_THRUST_STD::output_iterator_tag`), translating `::cuda::std::output_iterator_tag` per the existing convention (`thrust/iterator/iterator_categories.h`). numstat 6/0, exact match. Staged.
- [X] d568a2015a11eee5a66263dd45f49df178574340 Bump branch/3.1.x to 3.1.3. (#6621) [SCOPE: HEADER] — OUT OF SCOPE, no changes
  - Pure version-bump commit (CCCL 3.1.2 → 3.1.3), same category as `1128b6f797`/`2f940a0c57`. Confirmed with the user: no changes.
- [X] b4864ff3b4742b32f36df6420798e7dfafc1ead4 Bump branch/3.1.x to 3.1.4. (#6764) [SCOPE: HEADER] — OUT OF SCOPE, no changes
  - Pure version-bump commit (CCCL 3.1.3 → 3.1.4, the final release in this sync's target range). Same category as `1128b6f797`/`2f940a0c57`/`d568a2015a`. Confirmed with the user: no changes.
