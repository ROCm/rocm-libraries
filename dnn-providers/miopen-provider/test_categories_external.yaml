# Copyright © Advanced Micro Devices, Inc., or its affiliates.
# SPDX-License-Identifier: MIT
#
# miopen-provider CTest Categories (RFC0010).
#
# This file intentionally has no categories today. miopen-provider's public
# check targets are driven by the GTest-filtered categories in
# test_categories.yaml. Keep this file so providers still have a CTest-name YAML
# hook if a future non-GTest CTest test needs labels.
test_categories: {}
