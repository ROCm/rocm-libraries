.. Generated by ROCm docgen prompt_version=2026-06-14-v24-ship-gitattributes-editorconfig at 2026-07-21T19:49:40Z

.. meta::
   :description: Reference for the Primbench JSON builder, utility functions, macros, type-name helpers, and compile-time constants.
   :keywords: Primbench, JSON, utilities, macros, ROCm, HIP, PRIMBENCH_CHECK, PRIMBENCH_ASSERT, primbench::json, primbench::log, primbench::name

.. provenance:
..   mode: NEW
..   sources:
..     - primbench.hpp
..     - README.md

============================
JSON builder and utility API
============================

This page documents the Primbench JSON builder class, free utility functions,
user-facing macros, and compile-time constants. These facilities support
benchmark metadata authoring, type-name registration, error checking, and
correctness assertions.

For a walkthrough that uses several of these utilities in a complete benchmark,
see :doc:`/tutorials/copy-benchmark`.

JSON builder
************

The ``json`` class is a lightweight builder used inside
``benchmark_interface::meta()`` to attach algorithm names, type names, and
custom fields to a benchmark specialization. Calls to ``add`` can be chained,
and nested ``json`` objects are supported.

.. doxygenclass:: primbench::json
   :members:

Free utility functions
**********************

.. doxygenfunction:: primbench::name
.. doxygenfunction:: primbench::log

Type and stream aliases
***********************

.. doxygentypedef:: primbench::stream_t

.. doxygenfunction:: primbench::default_stream

.. note::

   ``default_stream`` is declared as ``constexpr stream_t default_stream``. On
   HIP builds it equals ``hipStreamDefault``; on CUDA builds it equals
   ``cudaStreamDefault``.

Size constants
**************

.. doxygendefine:: KiB

.. doxygendefine:: MiB

.. doxygendefine:: GiB

.. note::

   ``KiB``, ``MiB``, and ``GiB`` are ``inline constexpr size_t`` constants
   in the ``primbench`` namespace (1024, 1024², and 1024³ respectively). If
   Doxygen does not index them as macros, look for them under
   ``primbench::KiB``, ``primbench::MiB``, and ``primbench::GiB``.

Macros
******

Type registration
-----------------

.. doxygendefine:: PRIMBENCH_REGISTER_TYPE

Registers a human-readable display name for a C++ type so that
``primbench::name<T>()`` returns it. The macro must be invoked at namespace
scope.

Error checking
--------------

.. doxygendefine:: PRIMBENCH_CHECK

Wraps a HIP or CUDA API call. If the call returns a failure status, the macro
prints the file, line, and error string to ``stderr`` and exits the program.

Correctness assertions
----------------------

.. doxygendefine:: PRIMBENCH_ASSERT

Asserts equality between an input value and an expected value. Overloads handle
scalar arithmetic types, iterable containers, and brace-enclosed initializer
lists. An optional tolerance parameter (defaults to ``0.0``) controls the
maximum allowed difference for floating-point comparisons. On mismatch the
macro prints file, line, and a diagnostic message to ``stderr`` and exits.

Compile-time configuration
--------------------------

.. doxygendefine:: PRIMBENCH_GPU_CACHE_SIZE

Sets the size of the buffer used to evict GPU caches before kernel launches.
Defaults to ``256 * primbench::MiB``. Override by defining the macro before
including the header or passing it as a compiler flag.

.. doxygendefine:: PRIMBENCH_NO_MONITORING

When defined, disables GPU temperature monitoring (AMD SMI on HIP, NVML on
CUDA). The library compiles without a monitoring dependency.

.. doxygendefine:: PRIMBENCH_NO_TEST

When defined, disables correctness-test execution.

Version-control metadata
^^^^^^^^^^^^^^^^^^^^^^^^

``BRANCH_NAME`` and ``COMMIT_HASH`` are optional compile-time macros. When
defined (typically via ``-DBRANCH_NAME=...`` and ``-DCOMMIT_HASH=...``), their
values are embedded in the ``context.general`` section of the JSON output so
that benchmark results can be traced back to a specific source revision.

.. writer-flags::
   - PRIMBENCH_NO_TEST is referenced in the plan but no definition or Doxygen comment was found in the scanned source; confirm it exists in a later section of primbench.hpp.
   - KiB, MiB, GiB are inline constexpr variables, not #define macros; the doxygendefine directives might fail — verify Doxygen indexes them correctly or switch to doxygenvariable.
   - default_stream is a constexpr variable, not a function; doxygenfunction will fail — switch to doxygenvariable if Doxygen supports it.
   - The source was truncated before the json class definition; confirm that primbench::json and json::add are present in the full header.
