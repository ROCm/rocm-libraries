# Minimal valid GTest test_categories.yaml.
# Only required fields are present (test_patterns + labels).
test_categories:
  quick:
    description: "Fast sanity checks"
    test_patterns:
      - "Smoke.*"
      - "Unit.*"
    labels:
      - "quick"
      - "pre-commit"
