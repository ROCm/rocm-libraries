P2 UNIT GATE — full -m unit, 0 failed (after the findConfigs auto-discovery fix)
================================================================================
Date: 2026-06-06
Branch: users/davidd-amd/tensillite-coverage
Command (in-container):
  CON=tl-char; PROJ=/work/projects/hipblaslt/tensilelite
  docker exec -e PYTHONPATH=$PROJ -w $PROJ $CON pytest -p no:cacheprovider -m unit -q

RESULT:
  2528 passed, 201 skipped, 350 deselected, 10 warnings in 207.42s
  713 snapshots passed.
  => 0 failed. +15 passed vs the 2513 in-scope baseline = the 15 new seed tests.

BLOCKER THAT WAS FIXED
----------------------
The widened-P2 Assemble run STOPPED with 5 failures in
Tensile/Tests/common/test_config.py::test_config[.../_designed/<arch>/*.yaml].

Root cause: test_config is parametrized over config_helpers.findConfigs(), which os.walk()s the
whole Tensile/Tests tree and runs every *.yaml through the GPU Tensile.Tensile() path
(detectGlobalCurrentISA fails in this CPU-only container). The designed seed YAMLs are valid
Tensile configs (top-level dicts), so the existing uncommitted config_helpers.py guard
(skip non-dict / underscore-dir) does NOT exclude them — they got auto-discovered and run as GPU
benchmark configs.

Fix (ADD-ONLY, does NOT touch config_helpers.py): findConfigs already skips any path containing
the substring "test_data" (config_helpers.py: `if not "test_data" in filepath`). Relocated the 5
designed seed YAMLs from
  _codegen/data/_designed/<arch>/*.yaml
to
  _codegen/data/test_data/_designed/<arch>/*.yaml
and inserted a "test_data" path component in the 5 referencing tests
(test_seed_gfx942_char, _gfx942_hss_char, _gfx950_char, _gfx90a_char, _gfx90a_db_char).
The other 10 seed tests reference existing Tests/common/** configs and were unaffected.

VERIFICATION:
- The 5 relocated seed tests still emit real assembly, all err==0 (5 passed in 57.52s).
- test_config no longer collects any _designed seed (collect-only grep "_designed" => 0).
- Full -m unit: 0 failed (above).
- config_helpers.py left modified+uncommitted in the working tree (untouched by this campaign).
