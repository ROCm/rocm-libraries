{
  "id": "0d3cd6b0f66334e1de87f9308a6b6fa141445428",
  "file": "Tensile/BenchmarkProblems.py",
  "function": "_generateCustomKernelSolutions",
  "branch_kind": "if",
  "location": {
    "line": 304,
    "col": 12
  },
  "predicate_source": "failOnMismatch",
  "domains": {
    "failOnMismatch": {
      "type": "bool",
      "values": [
        true,
        false
      ]
    },
    "benchmarkStep.customKernelWildcard": {
      "type": "bool",
      "values": [
        false,
        true
      ]
    },
    "BenchmarkProblems[N][1+].CustomKernels": {
      "type": "enum",
      "values": [
        [],
        [
          "some_kernel_name"
        ],
        [
          "*"
        ]
      ]
    }
  },
  "branch_id": "0d3cd6b0f66334e1de87f9308a6b6fa141445428",
  "solver": "z3",
  "solver_status": "sat",
  "classification": "fully-static",
  "encoding": {
    "sort": "EnumSort(CustomKernels, [empty, named, star])",
    "var": "ck : CustomKernels   (root public-input field BenchmarkProblems[N][1+].CustomKernels)",
    "derivation": [
      "wildcard       = (ck == star)    # BenchmarkStructs.py:316-318  customKernelWildcard = (customKernels == ['*'])",
      "failOnMismatch = Not(wildcard)   # BenchmarkProblems.py:576     failOnMismatch = not benchmarkStep.customKernelWildcard"
    ],
    "predicate": "Not(ck == star)",
    "op_surface": [
      "compare(==)",
      "unary(not)"
    ],
    "note": "Pure boolean: equality of the YAML CustomKernels list against the wildcard literal ['*'], negated. Mirrors ExpressionEvaluator compare+unary ops. z3 exhaustively decided over the complete seeded enum domain {empty, named, star}; all three members checked."
  },
  "true_examples": [
    {
      "BenchmarkProblems[N][1+].CustomKernels": [],
      "benchmarkStep.customKernelWildcard": false,
      "failOnMismatch": true,
      "eval": "not ([] == ['*']) == True",
      "source": "z3-model"
    },
    {
      "BenchmarkProblems[N][1+].CustomKernels": [
        "some_kernel_name"
      ],
      "benchmarkStep.customKernelWildcard": false,
      "failOnMismatch": true,
      "eval": "not (['some_kernel_name'] == ['*']) == True",
      "source": "live-constructor (BenchmarkStep.__init__)"
    }
  ],
  "false_examples": [
    {
      "BenchmarkProblems[N][1+].CustomKernels": [
        "*"
      ],
      "benchmarkStep.customKernelWildcard": true,
      "failOnMismatch": false,
      "eval": "not (['*'] == ['*']) == False",
      "source": "z3-model"
    }
  ],
  "pure_helper": "from typing import List\n\ndef fail_on_mismatch(custom_kernels: List[str]) -> bool:\n    \"\"\"Pin of BenchmarkProblems.py:304 predicate over its public-input derivation chain.\n\n    Mirrors BenchmarkStructs.BenchmarkStep.__init__ (lines 315-318) +\n    the _generateProblemBenchmarkSet call site (BenchmarkProblems.py:576):\n        customKernelWildcard = (customKernels == [\"*\"])\n        failOnMismatch       = not customKernelWildcard\n    Returns True when the line-304 branch (raise/printExit-on-mismatch) is active.\n    \"\"\"\n    custom_kernel_wildcard = (custom_kernels == [\"*\"])\n    return not custom_kernel_wildcard",
  "crosshair": {
    "ran": true,
    "version": "0.0.106",
    "contract": "post: __return__ == (custom_kernels != ['*'])",
    "result": "no counterexample within per_condition_timeout=30s (exit 0)",
    "caveat": "Absence of a counterexample is NOT a proof. Dispositive evidence is the z3 exhaustive enum decision + live BenchmarkStep.__init__ verification."
  },
  "verification": "Verified against the REAL Tensile.BenchmarkStructs.BenchmarkStep constructor (getAllCustomKernelNames stubbed, minimal problemSizes stub): CustomKernels=[] -> wildcard=False failOnMismatch=True ; CustomKernels=['some_kernel_name'] -> wildcard=False failOnMismatch=True ; CustomKernels=['*'] -> wildcard=True failOnMismatch=False. Matches the z3 model exactly.",
  "classification_note": "fully-static: failOnMismatch is a pure boolean derived solely from the public YAML field CustomKernels (BenchmarkProblems[N][1+].CustomKernels). failOnMismatch == (CustomKernels != ['*']). A config author drives this branch either way from public input alone (no GPU/os/filesystem probe in the predicate itself). REACHABILITY CAVEAT (not on the predicate): line 304 sits inside two enclosing conditions - (a) the loop body runs once per name in customKernels, so CustomKernels=[] yields zero iterations and line 304 is never evaluated at runtime even though failOnMismatch would be True; and (b) the line-303 guard 'if solution[ProblemType] != problemType:' which depends on the parsed custom-kernel file ProblemType vs the config ProblemType (a separate, file/parse-dependent branch). The TRUE witness that actually EXERCISES line 304 is CustomKernels=['some_kernel_name'] with a mismatching ProblemType; the empty-list TRUE model is logically valid for the predicate but vacuous for reachability. The FALSE witness CustomKernels=['*'] expands to all custom kernel names with failOnMismatch=False, so mismatches are silently rejected (line 322) instead of fatal.",
  "frag_path": "work/tensilelite-characterization/parametric-chaos/PublicInputSurfaceDeep/_frags/Solve/0d3cd6b0f66334e1de87f9308a6b6fa141445428.json"
}