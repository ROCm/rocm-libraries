{
  "branch_id": "4480256b782f32e867dd3bf96665dc8ae5b6a883",
  "predicate_site": "Tensile/KernelWriterAssembly.py:7089",
  "predicate_source": "tailLoop",
  "function": "calculateLoopNumIter",
  "branch_kind": "if",
  "solver": "z3",
  "solver_version": "4.16.0",
  "solver_status": "sat",
  "classification": "solver-backed-under-assumptions",
  "encoding": {
    "op_surface": "single integer compare (lt) with a constant: tailLoop = (loopIdx < 0). One operator (comparison <); no arith/bit/conditional. The if at 7089 tests the resulting bool directly. NoTailLoop = (ASEM % DepthU == 0) is encoded as a reachability assumption (mod + eq).",
    "z3_decl": "loopIdx = Int(\"loopIdx\"); AssertSummationElementMultiple = Int(\"AssertSummationElementMultiple\"); DepthU = Int(\"DepthU\")",
    "predicate": "tailLoop == (loopIdx < 0)",
    "reachability_assumption": "NoTailLoop == (AssertSummationElementMultiple % DepthU == 0); the loopIdx=-1 (tail) call is only emitted when NoTailLoop is False (KernelWriter.py:5352 guards the tail-loop section with `if not kernel[NoTailLoop]`).",
    "domain": {
      "loopIdx": { "type": "int", "values": [-1, 0] },
      "AssertSummationElementMultiple": { "type": "int", "values": [8, 12] },
      "DepthU": { "type": "int", "values": [8, 16] }
    }
  },
  "true_examples": [
    { "loopIdx": -1, "AssertSummationElementMultiple": 12, "DepthU": 8, "tailLoop": true, "NoTailLoop": false }
  ],
  "false_examples": [
    { "loopIdx": 0, "AssertSummationElementMultiple": 8, "DepthU": 8, "tailLoop": false, "NoTailLoop": true }
  ],
  "z3_model_true": {
    "loopIdx": "-1",
    "AssertSummationElementMultiple": "12",
    "DepthU": "8",
    "tailLoop": "True",
    "NoTailLoop": "False",
    "check": "sat"
  },
  "z3_model_false": {
    "loopIdx": "0",
    "AssertSummationElementMultiple": "8",
    "DepthU": "8",
    "tailLoop": "False",
    "NoTailLoop": "True",
    "check": "sat"
  },
  "pure_helper": "def is_tail_loop(loopIdx: int) -> bool:\n    \"\"\"Mirror of KernelWriterAssembly.calculateLoopNumIter line 7081:\n       tailLoop = loopIdx < 0 ; the predicate at 7089 is `if tailLoop`.\n    post: __return__ == (loopIdx < 0)\n    \"\"\"\n    return loopIdx < 0",
  "crosshair": {
    "version": "0.0.106",
    "checked": "is_tail_loop(loopIdx: int) -> bool with post: __return__ == (loopIdx < 0)",
    "result": "no counterexample (exit 0)",
    "caveat": "CrossHair absence-of-counterexample is NOT a proof; it is bounded symbolic exploration. The helper is trivial (single compare) so confidence is high, but this is not a formal guarantee."
  },
  "runtime_note": "z3 4.16.0 proves both polarities satisfiable over the seeded domain. The predicate itself is a pure integer comparison: tailLoop = (loopIdx < 0), tested by `if tailLoop` at line 7089. loopIdx is a call-site convention argument, NOT a direct YAML input: callers pass -1 to request the tail-loop path and a non-negative summation-loop index otherwise (docstring at 7074-7079). TRUE witness loopIdx=-1 -> tailLoop True (tail-loop code section entered). FALSE witness loopIdx=0 -> tailLoop False (summation-loop path; tail section skipped). classification=solver-backed-under-assumptions because the *interesting reachability question* (when is the loopIdx=-1 call actually emitted?) depends on the derived kernel param NoTailLoop = (AssertSummationElementMultiple % DepthU == 0): KernelWriter.py:5352 gates the entire tail section with `if not kernel[NoTailLoop]`, so a -1 call is reachable only when NoTailLoop is False. Over the seeded domain {ASEM in {8,12}, DepthU in {8,16}}: 8%8==0 -> NoTailLoop True (no tail call), 8%16==8, 12%8==4, 12%16==12 -> NoTailLoop False (tail call emitted). The TRUE witness (ASEM=12,DepthU=8) is therefore a *reachable* tail call; the FALSE witness (ASEM=8,DepthU=8) coincides with NoTailLoop True where no tail call occurs and only the summation path (loopIdx>=0) runs. No os.environ/filesystem/GPU probe gates loopIdx or NoTailLoop; both reduce to YAML solution ints, hence solver-backed (not runtime-dependent). The under-assumptions qualifier covers the call-site convention for loopIdx and the NoTailLoop reachability gate, neither of which is a raw public input but both reduce deterministically to seeded ints.",
  "frag_path": "work/tensilelite-characterization/parametric-chaos/CodegenResidue/_frags/Solve/4480256b782f32e867dd3bf96665dc8ae5b6a883.json"
}
