{
  "branch_id": "0902ebf1cd3edc0bc1aac9c97f8dfd41cba0c700",
  "predicate_site": "Tensile/KernelWriterAssembly.py:4250",
  "predicate_source": "self.states.groOffsetInMacroTile",
  "function": "computeLoadSrd",
  "branch_kind": "if",
  "solver": "z3",
  "solver_version": "4.16.0",
  "solver_status": "sat",
  "classification": "solver-backed-under-assumptions",
  "encoding": {
    "op_surface": "single 'truthy' test on an attribute read self.states.groOffsetInMacroTile (no compare/arith/bit/conditional operator at the site; the operand is an int field). Encoded as integer != 0 over the seeded domain {0,1}.",
    "z3_decl": "groOffsetInMacroTile = Int(\"groOffsetInMacroTile\")  # seeded int in {0,1}",
    "predicate": "P == (groOffsetInMacroTile != 0)",
    "domain": {
      "self.states.groOffsetInMacroTile": {
        "type": "int",
        "values": [
          0,
          1
        ]
      }
    },
    "derivation_encoding": {
      "note": "groOffsetInMacroTile is a DERIVED int, not a public input. KernelWriter._initKernel (KernelWriter.py:6631): groOffsetInMacroTile = 1 iff len(PackedC0IndicesX)==1 AND len(PackedC1IndicesX)==1 AND BufferLoad, else 0. Encoded over the abstracted public-input surface (packed-index list LENGTHS + BufferLoad bool) so witnesses can be expressed at the public-input level.",
      "z3_decl": "lenPC0=Int; lenPC1=Int (bounded 0..4); BufferLoad=Bool; gro==If(And(lenPC0==1,lenPC1==1,BufferLoad),1,0)",
      "predicate": "P == (gro != 0)"
    }
  },
  "true_examples": [
    {
      "self.states.groOffsetInMacroTile": 1
    },
    {
      "BufferLoad": true,
      "lenPackedC0IndicesX": 1,
      "lenPackedC1IndicesX": 1,
      "_derived_gro": 1,
      "_note": "public-input-level TRUE witness: BufferLoad on and exactly one packed index on each of C0/C1 => groOffsetInMacroTile=1 => branch taken"
    }
  ],
  "false_examples": [
    {
      "self.states.groOffsetInMacroTile": 0
    },
    {
      "BufferLoad": true,
      "lenPackedC0IndicesX": 2,
      "lenPackedC1IndicesX": 2,
      "_derived_gro": 0,
      "_note": "public-input-level FALSE witness (packed dims): >1 packed index => groOffsetInMacroTile=0 => branch skipped (matches the source comment 'packed modes can't use this mode')"
    },
    {
      "BufferLoad": false,
      "lenPackedC0IndicesX": 1,
      "lenPackedC1IndicesX": 1,
      "_derived_gro": 0,
      "_note": "public-input-level FALSE witness (BufferLoad off): groOffsetInMacroTile=0 => branch skipped"
    }
  ],
  "z3_model_true": {
    "encoder_boundary": {
      "groOffsetInMacroTile": "1",
      "check": "sat"
    },
    "public_inputs": {
      "BufferLoad": "True",
      "lenPackedC0IndicesX": "1",
      "lenPackedC1IndicesX": "1",
      "gro": "1",
      "check": "sat"
    }
  },
  "z3_model_false": {
    "encoder_boundary": {
      "groOffsetInMacroTile": "0",
      "check": "sat"
    },
    "public_inputs_packed": {
      "BufferLoad": "True",
      "lenPackedC0IndicesX": "2",
      "lenPackedC1IndicesX": "2",
      "gro": "0",
      "check": "sat"
    },
    "public_inputs_bufferload_off": {
      "BufferLoad": "False",
      "lenPackedC0IndicesX": "1",
      "lenPackedC1IndicesX": "1",
      "gro": "0",
      "check": "sat"
    }
  },
  "pure_helper": "def gro_offset_in_macro_tile(len_packed_c0: int, len_packed_c1: int, buffer_load: bool) -> int:\n    \"\"\"Mirror KernelWriter._initKernel (KernelWriter.py:6631):\n    groOffsetInMacroTile = 1 iff len(PackedC0IndicesX)==1 and len(PackedC1IndicesX)==1 and BufferLoad, else 0.\n    The branch predicate at KernelWriterAssembly.py:4250 is truthy(groOffsetInMacroTile).\"\"\"\n    if len_packed_c0 == 1 and len_packed_c1 == 1 and buffer_load:\n        return 1\n    return 0",
  "crosshair": {
    "tool": "crosshair check",
    "contracts": [
      "post: __return__ in (0,1)",
      "post: (__return__==1) == (len_packed_c0==1 and len_packed_c1==1 and buffer_load)"
    ],
    "result": "no counterexample found (exit 0, per_condition_timeout=20s)",
    "caveat": "CrossHair absence-of-counterexample is NOT a proof; it is consistent with the z3 encoding and the source semantics."
  },
  "runtime_note": "z3 4.16.0 proves BOTH polarities satisfiable. At the encoder boundary the predicate is a pure truthy test on an int field seeded to {0,1}: P==True sat @ gro=1, P==False sat @ gro=0. groOffsetInMacroTile is NOT itself a public input - it is structurally derived in KernelWriter._initKernel (KernelWriter.py:6631) as 1 iff len(PackedC0IndicesX)==1 AND len(PackedC1IndicesX)==1 AND BufferLoad. The site (KernelWriterAssembly.py:4250) even re-asserts len(PackedC0IndicesX)==1 and len(PackedC1IndicesX)==1 inside the taken branch. Both polarities are genuinely reachable: TRUE when BufferLoad (YAML bool, default True) is on and each of PackedC0/C1IndicesX has exactly one entry (the common non-packed GEMM case); FALSE when BufferLoad is off OR a packed dimension makes a list length != 1. The packed-index lengths are themselves derived from IndexAssignmentsA/B, NumIndicesC, TransposeA/B, Batched (see Solution.py:1788-1814, Problem.py:1100-1107) - all YAML public inputs - so no os.environ/filesystem/GPU probe gates this branch. classification=solver-backed-under-assumptions: sat is established for both polarities at the encoder boundary under the seeded int domain {0,1}, and the public-input-level witnesses depend on the assumed structural derivation (BufferLoad bool + packed-index list lengths) rather than being re-derived end-to-end from raw YAML through Solution/Problem assignment.",
  "frag_path": "work/tensilelite-characterization/parametric-chaos/CodegenResidue/_frags/Solve/0902ebf1cd3edc0bc1aac9c97f8dfd41cba0c700.json"
}
