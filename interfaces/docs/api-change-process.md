# Adding or changing public APIs

This content has been absorbed into the documentation set to keep the change process in one
place with the contract it obeys:

- The **rules** (ABI header compatibility, SONAME major criteria, enum and record
  invariants) are in [03-abi-and-versioning-contract.md](03-abi-and-versioning-contract.md).
- The **step-by-step recipes** (non-breaking addition, breaking change, draft-to-launch
  drift) are in [05-extending.md](05-extending.md), under "add or change a public API".

This stub remains so existing links do not break.
