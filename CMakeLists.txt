# Copyright Advanced Micro Devices, Inc., or its affiliates.
# SPDX-License-Identifier: MIT

cmake_minimum_required(VERSION 3.25.2)
project(rocm-libraries LANGUAGES C CXX Fortran ASM)

include(GNUInstallDirs)

# Options and cache variables
# ===================================
option(ROCM_LIBRARIES_SUPERBUILD "Enable superbuild mode" ON)

option(ENABLE_MXDATAGENERATOR "Enable mxdatagenerator" ON)
option(ENABLE_ROCROLLER "Enable rocroller" ON)
option(ENABLE_TENSILE "Enable tensile" ON)
option(ENABLE_HIPBLAS_COMMON "Enable hipblas-common" ON)

option(ENABLE_ROCPRIM "Enable rocprim" ON)
option(ENABLE_ROCRAND "Enable rocrand" ON)
option(ENABLE_ROCSPARSE "Enable rocsparse" ON)
option(ENABLE_ROCFFT "Enable rocfft" ON)
option(ENABLE_ROCTHRUST"Enable rocthrust" ON)

option(ENABLE_HIPCUB "Enable hipcub" ON)
option(ENABLE_HIPRAND "Enable hiprand" ON)
option(ENABLE_HIPFFT "Enable hipfft" ON)
option(ENABLE_HIPSPARSE "Enable hipsparse" ON)

option(ENABLE_CK "Enable composablekernel" ON)
option(ENABLE_MIOPEN "Enable miopen" ON)

option(ENABLE_HIPBLAS "Enable hipblas" OFF)
option(ENABLE_HIPBLASLT "Enable hipblaslt" OFF)
option(ENABLE_HIPSOLVER "Enable hipsolver" OFF)
option(ENABLE_HIPSPARSELT "Enable hipsparselt" OFF)
option(ENABLE_ROCBLAS "Enable rocblas" OFF)
option(ENABLE_ROCSOLVER "Enable rocsolver" OFF)

# NOTE: This is a temp option to reduce warnings generated by rocm-cmake.
# However, these warnings are reasonable and should be addressed.
option(ROCM_WARN_TOOLCHAIN_VAR "Warn if the toolchain version is not supported" OFF)

# Add subdirectories
# ===================================
if(ENABLE_MXDATAGENERATOR)
    message(STATUS "[rocm-libraries] Configuring mxdatagenerator")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/shared/mxdatagenerator)
    if(NOT TARGET roc::mxDataGenerator)
        message(FATAL_ERROR "[rocm-libraries] No target roc::mxDataGenerator")
    endif()
endif()

if(ENABLE_ROCROLLER)
    # required deps:
    #    pip install pytest-cmake
    message(STATUS "[rocm-libraries] Configuring rocroller")

    set(ROCROLLER_ENABLE_FETCH ON)
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/shared/rocroller)
    if(NOT TARGET roc::rocroller)
        message(FATAL_ERROR "[rocm-libraries] No target roc::rocroller")
    endif()
endif()

if(ENABLE_TENSILE)
    message(STATUS "[rocm-libraries] Configuring tensile")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/shared/tensile/next-cmake)
    if(NOT TARGET roc::tensile-host)
        message(FATAL_ERROR "[rocm-libraries] No target roc::tensile-host")
    endif()
endif()

if(ENABLE_HIPBLAS_COMMON)
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/hipblas-common)
    if(NOT TARGET roc::hipblas-common)
        message(FATAL_ERROR "[rocm-libraries] No target roc::hipblas-common")
    endif()
endif()

if(ENABLE_ROCPRIM)
    message(STATUS "[rocm-libraries] Configuring rocprim")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/rocprim)
    if(NOT TARGET rocprim)
        message(FATAL_ERROR "[rocm-libraries] No target roc::rocprim")
    endif()
endif()

if(ENABLE_ROCRAND)
    message(STATUS "[rocm-libraries] Configuring rocrand")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/rocrand)
    if(NOT TARGET rocrand)
        message(FATAL_ERROR "[rocm-libraries] No target roc::rocrand")
    endif()
endif()

if(ENABLE_ROCTHRUST)
    message(STATUS "[rocm-libraries] Configuring rocthrust")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/rocthrust)
    if(NOT TARGET rocthrust)
        message(FATAL_ERROR "[rocm-libraries] No target roc::rocthrust")
    endif()
endif()

if(ENABLE_ROCFFT)
    message(STATUS "[rocm-libraries] Configuring rocfft")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/rocfft)
    if(NOT TARGET roc::rocfft)
        message(FATAL_ERROR "[rocm-libraries] No target roc::rocfft")
    endif()
endif()

if(ENABLE_ROCSPARSE)
    message(STATUS "[rocm-libraries] Configuring rocsparse")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/rocsparse)
    if(NOT TARGET roc::rocsparse)
        message(FATAL_ERROR "[rocm-libraries] No target roc::rocsparse")
    endif()
endif()

if(ENABLE_HIPCUB)
    message(STATUS "[rocm-libraries] Configuring hipcub")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/hipcub)
    if(NOT TARGET hipcub)
        message(FATAL_ERROR "[rocm-libraries] No target roc::hipcub")
    endif()
endif()

if(ENABLE_HIPRAND)
    message(STATUS "[rocm-libraries] Configuring hiprand")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/hiprand)
    if(NOT TARGET hip::hiprand)
        message(FATAL_ERROR "[rocm-libraries] No target roc::hiprand")
    endif()
endif()

if(ENABLE_HIPFFT)
    message(STATUS "[rocm-libraries] Configuring hipfft")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/hipfft)
    if(NOT TARGET hip::hipfft)
        message(FATAL_ERROR "[rocm-libraries] No target roc::hipfft")
    endif()
endif()

if(ENABLE_HIPSPARSE)
    message(STATUS "[rocm-libraries] Configuring hipsparse")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/hipsparse)
    if(NOT TARGET roc::hipsparse)
        message(FATAL_ERROR "[rocm-libraries] No target roc::hipsparse")
    endif()
endif()

if(ENABLE_CK)
    message(STATUS "[rocm-libraries] Configuring composablekernel")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/composablekernel)
endif()

if(ENABLE_MIOPEN)
    message(STATUS "[rocm-libraries] Configuring miopen")
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/miopen)
endif()

block()
    function(get_all_targets _result _dir)
        get_property(_subdirs DIRECTORY "${_dir}" PROPERTY SUBDIRECTORIES)
        foreach(_subdir IN LISTS _subdirs)
            get_all_targets(${_result} "${_subdir}")
        endforeach()

        get_directory_property(_sub_targets DIRECTORY "${_dir}" BUILDSYSTEM_TARGETS)
        set(${_result} ${${_result}} ${_sub_targets} PARENT_SCOPE)
    endfunction()
    get_all_targets(TARGETS_IN_DIR ${CMAKE_CURRENT_SOURCE_DIR})
    string(REPLACE ";" "\n" TARGETS_IN_DIR "${TARGETS_IN_DIR}")
    message(STATUS "Targets in directory: ${TARGETS_IN_DIR}")
endblock()


# Projects not yet configured for superbuild
# ==========================================
if(ENABLE_HIPBLASLT)
    message(FATAL_ERROR "hipblaslt is not yet supported in the superbuild")
    # add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/hipblaslt)
endif()

if(ENABLE_HIPBLAS)
    message(FATAL_ERROR "hipblas is not yet supported in the superbuild")
    # add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/hipblas)
endif()


if(ENABLE_HIPSOLVER)
    message(FATAL_ERROR "hipsolver is not yet supported in the superbuild")
    # add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/hipsolver)
endif()

if(ENABLE_HIPSPARSELT)
    message(FATAL_ERROR "hipsparselt is not yet supported in the superbuild")
    # add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/hipsparselt)
endif()


if(ENABLE_ROCBLAS)
    message(FATAL_ERROR "rocblas is not yet supported in the superbuild")
    # add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/rocblas)
endif()

if(ENABLE_ROCSOLVER)
    message(FATAL_ERROR "rocsolver is not yet supported in the superbuild")
    # add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/projects/rocsolver)
endif()
