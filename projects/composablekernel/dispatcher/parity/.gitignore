# Build artifacts (compiled by g++/hipcc; reproducible)
cpp_identifier_oracle
harness
libdispatcher_gemm.so

# Python caches
__pycache__/
*.pyc

# Codegen outputs (regenerated by drive_codegen.py)
generated/

# TE benchmark CSV output written by run_te_benchmark() on every GPU run.
# Accumulates in the parity directory after each run; not committed.
te_*.csv

# Sweep runner Parquet output (sweep_runner.py); can be large; regenerated on demand.
*.parquet

# Personal tracking docs (not part of the PR deliverables)
reportToEmin.md
reportToEmin.pdf

# Policy note: parity_design.pdf and parity_usage.pdf ARE intentionally committed.
# They are design/usage documentation generated by make_docs.py and kept in-tree
# so reviewers can read them without running the generator.  Binary PDF diffs are
# accepted for this repo (precedent: other subproject PDFs are committed similarly).
