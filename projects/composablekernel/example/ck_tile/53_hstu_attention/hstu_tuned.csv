# HSTU no-softmax jagged forward tuned-config override table.
#
# How it is used: this table is consulted before the legacy heuristic
# (get_hstu_attention_fwd_mtile / shall_use_splitkv). On a matching row whose
# tile signature is compiled into the binary, that kernel is launched; on any
# miss the call falls through to the unchanged heuristic. Delete this file (and
# leave $HSTU_TUNED_CONFIG unset) for pure legacy-heuristic behavior.
#
# Columns (one kernel selection per row):
#   dtype    : data type tag (e.g. bf16).
#   causal   : 1 causal, 0 non-causal.
#   B,H,D    : batch, num_head, head dim to match.
#   Nmin     : row fires when runtime max_seqlen_q >= Nmin.
#   mtile    : M-tile selection (e.g. 64 / 128). Note kM0 is NOT a column -- it
#              follows mtile via the compiled-in tile setting (mtile=64 -> kM0=192
#              bespoke max-reuse tile; mtile=128 -> kM0=128).
#   kn0,kn0sub,kn1,kk1 : block-tile dim overrides; 0 = base dim (no override,
#              use the compiled-in default for this (D->max_k, mtile)).
#   splitkv  : 1 enable split-KV, 0 disable.
#
# The rows below set mtile=64 (kM0=192) and override only kn0=32, leaving the
# other dims at base -> effective tile sequence<192,32,32,64,32,64>, WarpK=16.
#
# dtype,causal,B,H,Nmin,D,mtile,kn0,kn0sub,kn1,kk1,splitkv
bf16,1,120,4,8192,64,64,32,0,0,0,0
bf16,1,120,8,8192,64,64,32,0,0,0,0
bf16,1,1024,4,8192,64,64,32,0,0,0,0
bf16,1,1024,8,8192,64,64,32,0,0,0,0
