// Copyright © Advanced Micro Devices, Inc., or its affiliates.
// SPDX-License-Identifier: MIT

include "pooling_common.fbs";

namespace hipdnn_data_sdk.data_objects;

table PoolingBwdAttributes {
    // Input/Output tensor UIDs
    dy_tensor_uid: long;         // Gradient of output tensor (DY)
    dx_tensor_uid: long;         // Gradient of input tensor (DX)

    // Pooling parameters (N-dimensional support)
    pre_padding: [long];         // Padding before each spatial dimension
    post_padding: [long];        // Padding after each spatial dimension
    stride: [long];              // Stride for each spatial dimension
    window_size: [long];         // Pooling window size for each spatial dimension

    // Algorithm/mode selection
    pooling_mode: PoolingMode;
}
