PyYAML>=6.0
Jinja2>=3.1
# The runbook's step-7a gate is `$GEN/.venv/bin/python -m pytest .../test_fragment_struct_arity.py`,
# and several of the tools' own gates are pytest suites. Without this a venv built
# exactly as step 0 prescribes cannot run the checks the runbook then demands of it --
# `No module named pytest` at the one CI-shaped gate the runbook calls load-bearing.
pytest>=7.0
