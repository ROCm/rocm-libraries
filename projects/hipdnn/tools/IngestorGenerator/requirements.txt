PyYAML>=6.0
Jinja2>=3.1
# This generator's own gates are pytest suites -- `$GEN/.venv/bin/python -m pytest
# tests/test_fragment_contracts.py` and its siblings. Without this, a venv built exactly
# as the runbook's "Paths and interpreters" section prescribes cannot run them:
# `No module named pytest` at the checks the runbook then demands of it.
pytest>=7.0
