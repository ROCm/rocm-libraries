# Python packages for the hipSPARSELt build venv (see install.sh / tasks.py).
# Used so CMake find_package(Python3) picks an interpreter with PyYAML for
# hipsparselt_gentest.py and related tooling.
# joblib: required by hipBLASLt tensilelite/Tensile/Common/Parallel.py when
# hipSPARSELt CMake drives TensileLogic (see hipblaslt tensilelite/requirements.txt).
wheel
invoke
PyYAML>=5.1
joblib>=1.4.0
