# BEFORE coverage — existing suite (incl. the prior SolutionSupport slice) only,
# no SolutionClass tests yet. Total = 1800 passed.
#
# SLICE 2 of the Solution.py campaign: the Solution class construction +
# Mapping/identity + simple static helpers. Whole file is 3272 stmts at 38.81%
# line; the targeted symbols (Solution.__init__ happy path, the Mapping dunders,
# __str__/__hash__/__eq__, getMIOutputInfo / isDirectToVgprSupportDataType /
# getDivisorName, getKernels*) are largely uncovered at baseline.
#
# Command:
#   pytest -m unit --cov=Tensile --cov-config=pyproject.toml \
#     --cov-report=term-missing Tensile/Tests/unit

Name                                                      Stmts   Miss Branch BrPart   Cover
Tensile/SolutionStructs/Solution.py                         3272   2002   2234    432  34.25%

1800 passed, 201 skipped

# Whole-file line = (3272 - 2002) / 3272 = 38.81% line ; blended 34.25%
# Slice goal: >=95% line on the TARGETED slice-2 symbols (demonstrated in
# coverage-after.txt). Note: constructing a real Solution also exercises the
# derivation paths, so the whole-file number rises too.
