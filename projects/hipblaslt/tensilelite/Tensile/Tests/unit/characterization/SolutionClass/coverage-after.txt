# AFTER coverage — full `-m unit` suite WITH the new SolutionClass slice-2 suite
# (conftest + 1 test file, 18 tests, 14 snapshots).
#
# Command (same as before; --cov=Tensile path-mode + grep the row):
#   pytest -m unit --cov=Tensile --cov-config=pyproject.toml \
#     --cov-report=term-missing Tensile/Tests/unit

Name                                                      Stmts   Miss Branch BrPart   Cover
Tensile/SolutionStructs/Solution.py                         3272   1978   2234    433  34.89%

1818 passed, 201 skipped

# Whole-file line = (3272 - 1978) / 3272 = 39.55% line ; blended 34.89%
#
# SLICE 2 RESULT — the targeted Solution-class SURFACE symbols are fully covered:
#   getKernels (L534-543), getKernelBetaOlnyObjects/getKernelConversionObjects
#   (L544-553), getMIOutputInfo (L554-560), isDirectToVgprSupportDataType
#   (L1095-1103), getDivisorName (L1402-1414), the Mapping dunders (L5185-5199),
#   and __str__/__repr__/getAttributes/__hash__/__eq__ (L5201-5224).
# The ONLY targeted-symbol line still missing is L5229 (the __ne__
# `if result is NotImplemented: return result` arm) — dead, since __eq__ here
# always returns a concrete bool. See resistance.md.
#
# `Solution.__init__` / the parameter-derivation paths are exercised on the
# happy-path construction (which is why whole-file rose 38.81% -> 39.55%), but
# COMPREHENSIVE construction/derivation coverage is explicitly slice 3 — the bulk
# of the remaining Missing lines are those reject-heavy, config-branchy paths.
#
# No regression: 1800 -> 1818 passed (+18 new tests), 201 skipped unchanged.
# Additive only (new files under Tensile/Tests/unit/characterization/SolutionClass/).
