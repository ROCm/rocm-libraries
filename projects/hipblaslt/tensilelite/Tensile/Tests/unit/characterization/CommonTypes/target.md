# Characterization target — `Tensile/Common/Types.py`

Part of the master-plan remaining-module sweep. **Before 81.7% → after 100.00%
line** (60 stmts, 0 miss). Drives `IsaInfo`/`SemanticVersion`/`IsaVersion`/
`DebugConfig` and `makeDebugConfig` (empty / full / unknown-key configs cover
every `if "X" in config` branch). Pure; no resistance.
