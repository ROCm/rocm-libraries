# BEFORE coverage — existing suite only, no characterization tests for the
# Problem.py ProblemType slice yet (captured before the new ProblemType/ test
# files were collected; total = 1563 passed, unchanged from the post-
# ValidParameters baseline).
#
# NOTE: `--cov=Tensile/SolutionStructs/Problem` (path-prefix) fails to match a
# single .py file. Used the documented fallback `--cov=Tensile` and grepped the
# SolutionStructs/Problem.py row (see next-goal-problemtype.md).
#
# Command:
#   pytest -m unit --cov=Tensile --cov-config=pyproject.toml \
#     --cov-report=term-missing Tensile/Tests/unit
#
# This is a SLICE target: nearly the whole file IS the slice (ProblemSizeRange,
# Problem, ExactList/ExactDict, ProblemSizes(Mock), getRealDataType{A,B},
# problemTypeToEnum, validateProblemTypeParameterTypes, ProblemType,
# getBiasDataTypeListDefault). The existing suite drives ProblemType indirectly
# during Solution construction; the missing lines below are the un-exercised
# construction/naming/index branches + the ProblemSizes family.

Name                                                      Stmts   Miss Branch BrPart   Cover   Missing
Tensile/SolutionStructs/Problem.py                          601    240    332     64  55.63%   (see slice symbols 42-328, 379-397, 711-1382)

1563 passed, 201 skipped

# Line coverage = (Stmts - Miss) / Stmts = (601 - 240) / 601 = 60.07% line
# Blended (line+branch, the report's Cover column)            = 55.63%
