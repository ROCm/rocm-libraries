# AFTER coverage — full `-m unit` suite WITH the new SolutionSupport slice suite
# (conftest + 1 test file, 31 tests, 25 snapshots).
#
# Command (same as before; --cov=Tensile path-mode + grep the row):
#   pytest -m unit --cov=Tensile --cov-config=pyproject.toml \
#     --cov-report=term-missing Tensile/Tests/unit
#
# SLICE RESULT: the targeted pure support slice (L165-439) is now FULLY covered.
# In the AFTER Missing column the first missed line is 473 — i.e. there are NO
# missing lines in 165-439 (the cap-coupled Solution class L444+ remains out of
# slice and is the subject of the next campaign slice). The whole-file number is
# expected to stay low until those later slices land.

Name                                                      Stmts   Miss Branch BrPart   Cover   Missing
Tensile/SolutionStructs/Solution.py                         3272   2002   2234    432  34.25%   473, 500-501, ... (all >= 473, inside the out-of-slice Solution class)

1800 passed, 201 skipped

# Whole-file line = (3272 - 2002) / 3272 = 38.81% line ; blended 34.25%
#
# SLICE (L165-439) coverage = 100% line (0 missing lines in the targeted range;
# also 0 partial branches in-slice after the accumulate test).
#
# Before -> After (slice): the targeted symbols (collector merge, the arg
# dataclasses FactorDimArgs/BiasTypeArgs/ActivationArgs, isExtractableIndex,
# and the validate/print branches) went from largely-uncovered to 100%.
# No regression: 1769 -> 1800 passed (+31 new tests), 201 skipped unchanged.
# Additive only (new files under Tensile/Tests/unit/characterization/SolutionSupport/).
