# Copyright Advanced Micro Devices, Inc., or its affiliates.
# SPDX-License-Identifier: MIT
#
# w4a16 TN, fp16 activations (DataType/MacDataTypeA/DataTypeScaleA = h).
# fp16 has a real f32->f16 convert, so the dequantize's pack is three
# instructions instead of the open-coded bf16 rounding.
#
# w4a16 TN, ASYMMETRIC, Int4EncodingA=UnsignedBias8ExLlama:
#
# as UnsignedBias8, but each dword's 8 nibbles are interleaved
# [0,2,4,6,1,3,5,7] (the ExLlama / ExLlamaV2 shuffle). That puts elements
# 2k and 2k+1 in the low and high halves of the dword, so one mask+or
# lifts both at once and no v_cvt is needed to reach f32.
#
# Same numerics as
# w4a16_bs128_zp_tn.yaml, which runs the default Signed encoding; the two
# are meant to be compared against each other.
#
# ORIGINAL HEADER:
#
# w4a16 TN, ASYMMETRIC: int4 weights in A, bf16 activations in B, one bf16
# scale AND one signed int4 zero-point per 128 consecutive K elements of a row
# of A (UseScaleAB=Block, ScaleBlockSizeA=128, ScaleZeroPointA=True). The kernel
# computes (q - z)*s after the global load and before the LDS write, so the
# main loop is a plain bf16 GEMM.
#
# The zero-points are a second region of the same allocation, packed two per
# byte over the K-group-major flattened index e = g*roundUp(M,2) + m. Being
# K-group major over an even row count is what makes a thread's nibble depend
# only on its row, so DepthU/G need not be even.
#
# Constraints enforced by SolutionStructs/Validators/BlockDequant.py and
# reflected in the parameters below:
#   * TN only (A stored K-contiguous), BufferLoad, ConvertAfterDS=False
#   * exactly one dword (8 int4) per A global load -> GlobalReadVectorWidthA=8
#   * DepthU % ScaleBlockSizeA == 0
#   * no tail loop yet -> every K here is a multiple of DepthU
#   * no DepthU/ScaleBlockSizeA parity rule: the zero-points are K-group
#     major (e = g*roundUp(M,2) + m), so one group per iteration is fine
#   * GlobalSplitU=1, StaggerU=0, no TDM / DirectToVgpr / DirectToLds on A
TestParameters:
  marks: [skip-gfx900, skip-gfx906, skip-gfx908, skip-gfx90a, skip-gfx940, skip-gfx941, skip-gfx942, skip-gfx950, skip-gfx1010, skip-gfx1011, skip-gfx1012, skip-gfx1030, skip-gfx1100, skip-gfx1101, skip-gfx1102, skip-gfx1200, skip-gfx1201, skip-gfx1250]
GlobalParameters:
  SyncsPerBenchmark: 0
  NumElementsToValidate: -1
  KernelTime: False
  BoundsCheck: 0
  PrintSolutionRejectionReason: True
  ValidationMaxToPrint: 4
  ValidationPrintValids: False
  DataInitTypeA: 3
  # Random (3), not the default Two: a uniform scale tensor would pass
  # even if the kernel indexed the wrong K-group, so vary it per group.
  DataInitTypeScaleA: 3
  DataInitTypeB: 3
  DataInitTypeD: 3
  DataInitTypeAlpha: 1
  DataInitTypeBeta: 0
  DataInitSeed: 24681
  PrintLevel: 2
  ClientLogLevel: 2
  NumWarmups: 0
  EnqueuesPerSync: 1
  PrintWinnersOnly: False
  CpuThreads: -1

BenchmarkProblems:
  -
    - # ProblemType
      OperationType: GEMM
      DataType: h
      DataTypeA: i4
      MacDataTypeA: h
      DestDataType: h
      ComputeDataType: s
      HighPrecisionAccumulate: True
      TransposeA: True
      TransposeB: False
      UseBeta: True
      Batched: True
      UseScaleAB: Block
      ScaleBlockSizeA: 128
      ScaleZeroPointA: True
      Int4EncodingA: UnsignedBias8ExLlama
    - # BenchmarkProblemSizeGroup
      InitialSolutionParameters:
      BenchmarkCommonParameters:
        - KernelLanguage: ["Assembly"]
      ForkParameters:
        - MatrixInstruction:
          - [16, 16, 16, 1, 1, 1, 1, 2, 2]
        - GlobalReadVectorWidthA: [8]
        - GlobalReadVectorWidthB: [8]
        # gfx11 buffers local reads for every LoopIter (= DepthU/16), so VGPR
        # use scales as (MIWaveTile0+MIWaveTile1) * DepthU/2.
        - DepthU: [128]
        - WavefrontSize: [32]
        - StaggerU: [0]
        - GlobalSplitU: [1]
        - ScheduleIterAlg: [0]
        - PrefetchGlobalRead: [1]
        # gfx11 WMMA has MIInputPerThread == MatrixInstK == 16; with PLR>0 the
        # widest legal local read (8 bf16) is still narrower than one MI input
        # and is rejected, so this tile runs without local-read prefetch.
        - PrefetchLocalRead: [0]
        - ConvertAfterDS: [False]
        - AssertSummationElementMultiple: [128]
      BenchmarkForkParameters:
      JoinParameters:
      BenchmarkJoinParameters:
      BenchmarkFinalParameters:
        - ProblemSizes:
          - Exact: [64, 64, 1, 256]
          - Exact: [128, 128, 1, 512]
          - Exact: [192, 64, 1, 256]
          # Odd M: exercises the roundUp(M,2) pad in the zero-point layout.
          - Exact: [65, 64, 1, 256]
