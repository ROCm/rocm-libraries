# from llvm-project/libcxx/test/configs/cmake-bridge.cfg.in
# @AUTO_GEN_COMMENT@

# @SERIALIZED_LIT_PARAMS@
config.target_triple = 'x86_64-pc-linux-gnu'
config.std = 'c++17'
config.enable_experimental = False

import os, site

# Get paths from environment variables with fallbacks for local development
LLVM_PROJECT_DIR = os.environ.get('LLVM_PROJECT_DIR', '/home/marsavic/llvm-project')
GPULIB_SOURCE_DIR = os.environ.get('GPULIB_SOURCE_DIR', '/home/marsavic/gpulib')
GPULIB_BUILD_DIR = os.environ.get('GPULIB_BUILD_DIR', '/home/marsavic/gpulib/build')
ROCM_PATH = os.environ.get('ROCM_PATH', '/opt/rocm')

# Determine compiler path - prefer hipcc from ROCM_PATH, fallback to system
if os.path.exists(os.path.join(ROCM_PATH, 'bin', 'hipcc')):
    compiler_path = os.path.join(ROCM_PATH, 'bin', 'hipcc')
elif os.path.exists('/opt/rocm/bin/hipcc'):
    compiler_path = '/opt/rocm/bin/hipcc'
else:
    compiler_path = 'hipcc'  # Hope it's in PATH

config.compiler = compiler_path

site.addsitedir(os.path.join(LLVM_PROJECT_DIR, 'libcxx/utils'))
import libcxx.test.format

# Basic configuration of the test suite
config.name = 'hipThreads'
config.test_source_root = os.path.join(GPULIB_SOURCE_DIR, 'test')
config.test_format = libcxx.test.format.CxxStandardLibraryTest()
config.recursiveExpansionLimit = 10
config.test_exec_root = os.path.join(GPULIB_BUILD_DIR, 'test')

# Add substitutions for bootstrapping the test suite configuration
import shlex
config.substitutions.append(('%{cxx}', shlex.quote(compiler_path)))
#config.substitutions.append(('%{include}', '@LIBCXX_GENERATED_INCLUDE_DIR@'))
#config.substitutions.append(('%{lib}', '@LIBCXX_LIBRARY_DIR@'))
config.substitutions.append(('%{executor}', f'python3 {os.path.join(LLVM_PROJECT_DIR, "libcxx/utils/run.py")}'))
config.substitutions.append(('%{test-tools}', os.path.join(LLVM_PROJECT_DIR, 'libcxx/test/tools')))
config.substitutions.append(('%{benchmark_flags}', 
    f'-I {os.path.join(GPULIB_BUILD_DIR, "test/benchmarks/google-benchmark/include")} '
    f'-L {os.path.join(GPULIB_BUILD_DIR, "test/benchmarks/google-benchmark/lib")} '
    f'-L {os.path.join(GPULIB_BUILD_DIR, "test/benchmarks/google-benchmark/lib64")} '
    '-l benchmark'))


# from llvm-project/libcxx/test/configs/llvm-libc++-static.cfg.in
config.substitutions.append(('%{flags}',
    '-Wno-unused-command-line-argument -v -fgpu-rdc'
))

# Determine ROCm include path
rocm_include = os.path.join(ROCM_PATH, 'include')
if not os.path.exists(rocm_include):
    # Fallback to versioned ROCm path if it exists
    rocm_include = '/opt/rocm-7.0.2/include' if os.path.exists('/opt/rocm-7.0.2/include') else '/opt/rocm/include'

# Determine libhipcxx include path
# In CI (venv setup): ROCM_PATH contains site-packages, libhipcxx is at venv base include
# Locally: libhipcxx is at /opt/rocm/include (already covered by rocm_include)
libhipcxx_include = ''
if 'site-packages' in ROCM_PATH:
    # Extract venv base path from ROCM_PATH
    # e.g., /path/to/.venv/lib/python3.12/site-packages/_rocm_sdk_devel -> /path/to/.venv
    venv_base = ROCM_PATH.split('/lib/python')[0] if '/lib/python' in ROCM_PATH else ROCM_PATH
    venv_include = os.path.join(venv_base, 'include')
    if os.path.exists(venv_include):
        libhipcxx_include = f'-isystem {venv_include} '

config.substitutions.append(('%{compile_flags}',
    f'-D_LIBCUDACXX_ALLOW_UNSUPPORTED_ARCHITECTURE -isystem {rocm_include} '
    f'{libhipcxx_include}'
    f'-I {os.path.join(GPULIB_SOURCE_DIR, "inc")} '
    f'-I {os.path.join(GPULIB_SOURCE_DIR, "test/support")} -flax-vector-conversions=all'
))
config.substitutions.append(('%{link_flags}',
#     '-L %{lib} -Wl,-rpath,%{lib} -lc++ -pthread'
    f'-latomic -lhipthreads -L {os.path.join(GPULIB_BUILD_DIR, "lib")}'
))
config.substitutions.append(('%{exec}',
    'mkdir -p %t.d && %{executor} --execdir %t.d -- '
))

if 'LD_LIBRARY_PATH' in os.environ:
    config.environment['LD_LIBRARY_PATH'] = os.environ['LD_LIBRARY_PATH']
else:
    config.environment['LD_LIBRARY_PATH'] = os.path.join(ROCM_PATH, 'lib')

import libcxx.test.params, libcxx.test.config
libcxx.test.config.configure(
    libcxx.test.params.DEFAULT_PARAMETERS,
    libcxx.test.features.DEFAULT_FEATURES,
    config,
    lit_config
)

# Post-process substitutions to enforce -flax-vector-conversions=all
# We iterate through the substitutions and append the flag to %{compile_flags} 
# (or replace 'none' if found) to ensure it overrides the defaults added by configure.
new_substitutions = []
for (key, value) in config.substitutions:
    if key == '%{compile_flags}':
        # If 'none' is present, replace it; otherwise append 'all' to the end
        if '-flax-vector-conversions=none' in value:
            value = value.replace('-flax-vector-conversions=none', '-flax-vector-conversions=all')
        else:
            value = value + ' -flax-vector-conversions=all'

        # Ensure libhipcxx "#warning" doesn't become an error under -Werror
        if '-Wno-error=#warnings' not in value:
            value = value + ' -Wno-error=#warnings'

    if key == '%{flags}':
        # Put this late so it can override a later-added -Werror
        if '-Wno-error=#warnings' not in value:
            value = value + ' -Wno-error=#warnings'
            
    new_substitutions.append((key, value))
config.substitutions = new_substitutions

# Should we include all the other bits in libhipcxx/test/lit.site.cfg.in too?

# build libcxx first:
# cmake -G Ninja -S runtimes -B build -DLLVM_ENABLE_RUNTIMES="libcxx;libcxxabi;libunwind"
# ninja -C build cxx cxxabi unwind
# ninja -v -C build check-cxx # I don't know if this is required
# then
# /home/daniel/llvm-project/build/bin/llvm-lit -sv --show-xfail --show-unsupported --param compiler=/opt/rocm/bin/hipcc ./test
